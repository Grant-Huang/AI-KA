# AI-KA 使用帮助

## 功能简介

AI-KA ProjectLens 是一个**通用的业务关联性文档审查**与**结构化分析**工具。你可以把它理解成「可配置的文档审查助手」：系统依据当前**审查技能包**内 `review_domain.md` 中定义的关注点与 Prompt 进行审查，因此不仅适用于项目交付实施，也可用于流程制度、产品需求、合规材料、运营规则、接口契约等业务文档。

**能力概览**

- 将项目目录中的文档转换为可检索的 Markdown（docs2md 等管线）
- 结合 `review_domain.md` 中的「关注点（focus）」（名称 + Prompt）对文档做审查
- 流式输出分析过程与结构化结论，并支持导出（如 Markdown）

**典型用途（以项目交付类场景为例）**

- 蓝图评审前：检查需求完整性、接口依赖、主数据一致性  
- 上线前：识别风险、进度阻塞与验收缺口  
- 复盘阶段：沉淀可复用经验与问题模式

更多设计与扩展思路见仓库内 `README.md` 与 `docs/ProjectLens_需求与设计文档_V1.0.md`。

## 审查技能包（Skills）与记忆（Memory）如何工作

### 审查技能包（Skills）

- **是什么**：一套可切换的**审查配置包**，内含 `manifest.json` 与 **`review_domain.md`**（关注点 `### focus:…`、**组合使用建议**五列表等）。决定「审什么、怎么审、输出结构」主要来自此文件及你在 **设置 → 审查域** 中保存的内容。
- **放在哪**：默认在仓库根目录 **`review_skill_packages/<包 id>/`** 下；可用环境变量 **`AIKA_REVIEW_SKILL_PACKAGES_ROOT`** 指向其它根。当前选中的包 id 由应用设置中的 **`active_skill_package_id`** 决定（界面在 **设置 → 审查域** 切换）。
- **怎么进到模型里**：分析时，后端会根据用户输入自动路由关注点组合，并把关注点 Prompt、技能包相关元数据（可复现用的版本与规则哈希等）写入 **系统提示词**，再与文档分块一起送给大模型。

### 记忆（Memory）

- **是什么**：存放在本机/服务端仓库根下 **`.aika/memory/`** 中的 **Markdown（`*.md`）** 短文本，用于在审查前补充**术语、偏好、项目画像**等，与当前文档分块分开维护。
- **目录约定（摘要）**：`user/`、`feedback/`（偏全局）；`project/<项目数字 id>/`、`reference/<项目数字 id>/`（按项目）。完整分类说明见 `docs/aika_spec/MEMORY_TAXONOMY.md`。
- **怎么被用上**：发起分析时，后端在允许目录下扫描候选 `.md`，按**检索词**做关键词匹配打分，选出少量片段，写入系统提示中的 **「【注入记忆片段】」** 段。当前版本的分析页不暴露“记忆检索”输入框；系统会根据用户问题与已路由的关注点自动构造检索词。
- **怎么确认生效**：审查流式输出结束后，若本轮确实注入了记忆，在流程区 **「思考分析」** 折叠段末尾会追加 **「【加载的记忆】」** 列表，列出本次命中的**相对路径**与**标题（文件名 stem）** 摘要（完整正文已进系统提示，不在此重复展示）。

### 工具（Tools）与钩子（Hooks）（简要）

- **Tools**：后端提供 **`POST /api/v1/tools/invoke`** 供脚本/集成调用（如校验 `review_domain` 文本），**日常点「开始」审查不必使用**。
- **Hooks**：服务端在每次 **`analyze/stream`** 分析前/后执行已注册逻辑（默认只打审计日志）；用于扩展时由开发者在后端注册，**界面无单独配置项**。

更系统的说明见《ProjectLens_需求与设计文档_V1.0.md》**第十二章**与《ProjectLens_详细设计_V1.1.md》**第九章**。

## 操作流程

1. 在左侧进入 **「项目初始化」**，选择目录（或手动输入路径）注册项目。
2. 点击 **「初始化（转换 + 索引）」** 完成语料准备（docs2md 转换 + 索引与分块）。
3. 切回 **「分析」**，选择已初始化项目，在输入框描述你的目标与关注点（可选），点击 **「开始」**。
4. 系统会自动编排本轮执行：识别是否需要澄清、选择合适的关注点组合、召回记忆、并生成审查结果与下载入口。
5. 分析完成后可继续多轮追问；若检测到语料缺失/过期，会提示你回到「项目初始化」重新建立索引。

**说明**：分析页会按会话保存多轮对话；若你在会话历史中打开了“语料已过期”的历史会话，则仅可回看，无法继续审查/追问。

**使用提示（精简）**

- 先维护好当前活动审查技能包内 `review_domain.md` 的关注点与「组合使用建议」，再跑全流程，结果更稳定。  
- 修改 **分块策略或索引模式** 后，务必重新执行 **索引**（及后续分析），否则结论仍可能基于旧分块。  
- 在 **设置 → 模型** 中核对文本 / VL 的 Base URL、模型名与 Key 是否与环境一致。  
- 若审查结果偏离预期，优先检查：关注点 Prompt 是否过泛、预设组合是否与当前文档类型匹配。

## 审查域说明

系统从**当前活动审查技能包**读取配置：包根目录默认为 `$AIKA_REPO_ROOT/review_skill_packages`，可用环境变量 `**AIKA_REVIEW_SKILL_PACKAGES_ROOT`** 覆盖。当前选中的包 id 保存在 `**app_settings.md**` 的 `**active_skill_package_id**`（默认 `package-general`）；在 **设置 → 审查域** 可切换包（保存后生效）。包目录与 `manifest.json` 约定见 `docs/aika_spec/PACKAGE_LAYOUT.md`。

**关注点（focus）**

每个关注点一个块，格式示例：

```md
### focus:req | 需求完整性
这里写该关注点对应的 Prompt，可多行。
```

- `focus:req`：关注点 ID（建议保持稳定，并与下方表格中 ``focus:...`` 引用一致）  
- `需求完整性`：展示名称  
- 正文：该关注点对应的 Prompt

**组合预设（「组合使用建议」）**

在 `review_domain.md` 中增加 **二级标题**（标题中须同时包含「组合」以及「建议」「预设」「搭配」「使用」等字样之一），并紧跟 **Markdown 表格**。系统仅解析 **固定五列表**（列序如下），并将表格行映射为首页与设置中的 **组合预设**（从 ``focus:id`` 解析关注点）。后三列用于 **审查角色**、**审查目标与原则**、**输出要求**，可留空；单元格内换行请写为 HTML `<br>`，单元格内需出现竖线 `|` 时请写为 `|`。

示例：

```md
## 组合使用建议

| 评审节点 | 推荐组合的关注点 | 审查角色 | 审查目标与原则 | 输出要求 |
| --- | --- | --- | --- | --- |
| 蓝图评审 | `focus:req` + `focus:integration` | | | |
```

导入或保存后，可在 **设置 → 审查域** 中检查关注点与预设是否与预期一致。

## 设置指南

入口：左侧边栏 **齿轮** 图标。设置分为三个页签：


| 页签      | 内容                                                                                                                                    |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| **文档**  | **分块**：chunk 上限、分块模式（空行分块 / 标题与结构感知）。**图片解析**：是否在 docs2md 转换时解析文档内图片。**文件更新（索引 md_out）**：增量索引或强制全量重建。修改分块或全量策略后，请按界面提示重新执行索引后再解读分析结果。 |
| **审查域** | 选择当前**审查技能包**，编辑 **关注点**、**组合预设**；支持 **导入审查域** 从本机选择 Markdown 写入当前活动包。                                                                |
| **模型**  | **文本大模型** 与 **VL 大模型**：Provider、Base URL、模型名、API Key（文本与 VL 可分别配置）。                                                                   |


关注点与部分选项写入当前包的 `review_domain.md`；其它应用项可能落在 `app_settings.md`（以当前版本行为为准）。**请勿将含密钥的文件提交到公开仓库。**

## 常见问题

### 1) 报错 `http error 404: 404 page not found`

多为 Base URL 与网关路径不一致。若填写带 `/v1` 的地址，请与服务商文档核对；同时检查模型名、兼容接口与 Key。

### 2) 导入审查域后没有「组合预设」

请确认文件中存在 **「组合使用建议」** 小节及表格，且推荐列使用 ``focus:关注点id`` 形式，且与上文 `### focus:...` 的 ID **完全一致**（含全角符号等细节）。导入成功后可在 **设置 → 审查域** 查看；当前版本分析页不要求用户选择预设，系统会自动路由关注点组合。

### 3) 为什么后台日志或流式输出为空

请确认已选择项目、预设，并已启动分析；若模型或网络异常，里程碑或日志中会有提示。

### 4) 没有配置 VL Key 会怎样

文本审查仍可进行；涉及图片解析时可能在日志中提示跳过或降级。

### 5) 打开页面后没有项目、没有选中预设

为默认行为：需自行通过 **「+」** 加载目录并 **选择预设** 后再开始分析；审查域内容由后端从当前活动包的 `review_domain.md` 加载（首次初始化默认包时，若尚无 `review_domain.md`，会从仓库根 `default_skills.md` 或内置占位生成，亦可用迁移接口导入）。