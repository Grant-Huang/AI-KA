# AI-KA 使用帮助

## 功能简介

AI-KA（ProjectLens）是一套**通用业务文档审查 + 知识沉淀**平台，面向咨询顾问、技术评审专家和知识管理团队，将项目文档审查与经验知识提炼融为一体。

### 两大核心模块

**项目审查模块（Review）**

- 基于项目目录中的 **Markdown（.md）** 文件构建可检索语料库
- 结合**审查技能包**中定义的关注点（Focus Points）对文档做结构化分析
- 流式输出分析里程碑与结论，支持多轮追问
- 自动注入项目记忆（术语、偏好、历史信息），在「思考分析」段显示命中片段
- 审查结束后可一键跳转到**知识归纳**模块，提炼可复用经验

**知识归纳模块（Extraction）**

- **归纳工作台**：AI 主动提问，引导专家把经验转化为结构化知识条目
- 四种提取策略：发现规则盲点 / 澄清模糊印象 / 复盘具体案例 / 挑战现有规则
- 支持上传参考文档，让 AI 基于文档内容做知识澄清
- 审查后提取：直接从上一次项目审查结论出发进行知识提炼
- **待批准规则**：人工审批 AI 提取的知识条目后写入活动技能包

### 典型用途

| 场景 | 模块 |
| --- | --- |
| 蓝图 / 需求 / 上线前快速文档审查 | 项目审查 |
| 复盘阶段提炼可复用经验 | 知识归纳（审查后提取） |
| 积累行业最佳实践 | 知识归纳（自由提取） |
| 文档知识澄清与结构化 | 知识归纳（上传参考文档） |
| 定期审批并入库知识规则 | 待批准规则 |

### 界面布局

左侧导航栏提供五个主要入口：

| 图标 | 功能 | 说明 |
| --- | --- | --- |
| 审计图标 | **项目审查** | 主审查对话界面 |
| 灯泡图标 | **知识归纳** | 知识提炼工作台与会话历史 |
| 图钉图标 | **待批准规则** | 审批 AI 提取的知识条目 |
| 文件夹图标 | **项目初始化** | 注册项目目录并建立文档索引 |
| 搜索图标 | **审查域设定** | 浏览和编辑关注点、组合预设、导入审查域 |

底部用户头像菜单包含：
- **设置**（文档分块与模型配置）
- **帮助与支持**（本帮助文档）

---

## 项目初始化

项目初始化是审查的第一步：将项目文档上传到系统、写入 Obsidian Vault、建立检索索引，之后才能在「分析」页发起审查。

### 推荐工作流（Vault 优先）

```
配置 Obsidian Vault → 上传文件初始化（自动转换）→ 建立索引 → 切换到分析
```

#### 第一步：配置 Obsidian Vault（强烈建议）

点击「项目初始化」页面顶部的 **「Obsidian Vault」** 按钮，注册一个本地 Obsidian Vault 目录。

> 若未配置 Vault，页面顶部会显示黄色警告横幅：文件将写入后端临时目录（`<repo>/.tmp/uploads/<项目名>/`），重新部署容器后会丢失。配置 Vault 后，文档持久化到 Vault 的 `Projects/<项目名>/` 子目录，Obsidian 也能直接打开编辑。

**Vault 角色说明：**

| 角色 | 用途 |
| --- | --- |
| `project`（项目文档）| 存放项目文档，用于项目审查语料 |
| `knowledge`（知识库）| 存放知识提取结果，用于知识积累 |
| `both`（两用）| 同时承担项目文档与知识库 |

**注册方式：**
- **自动发现**：弹窗中点击「扫描本机 Vault」，系统会扫描 `~/Documents/` 等常见目录，发现包含 `.obsidian/` 文件夹的目录
- **手动注册**：在输入框填入 Vault 目录的**绝对路径**，选择角色，点击「注册」

注册成功后，Vault 下会自动创建 `Projects/` 和 `_aika/` 子目录。

#### 第二步：上传文件初始化项目

点击 **「📤 上传文件初始化」** 按钮，进入两步引导弹窗：

**Step 1 — 选择目标 Vault**（仅 init 模式）
- 从下拉框中选择已注册的 Vault（仅显示角色为 `project` 或 `both` 的 Vault）
- 若跳过选择，文件写入后端临时目录（不推荐生产使用）
- 若尚未注册任何 Vault，点击弹窗内的「去配置」按钮先完成注册

**Step 2 — 选择文件**
- **「📁 选择文件夹」**：选择整个项目文件夹，保留目录层级结构
- **「🗂 选择多个文件」**：手动选择多个文件，平铺写入项目根目录
- 选完后弹窗显示文件类型摘要（直接写入 / 自动转换 / 未知格式）
- 填写**项目名称**（从文件夹名自动识别，可手动修改）
- 点击「上传并初始化」

**支持的文件格式：**

| 格式 | 处理方式 |
| --- | --- |
| `.md` `.txt` `.markdown` | 直接写入，不做转换 |
| `.docx` `.doc` | 调用 markitdown 转为 Markdown |
| `.pdf` | 调用 markitdown 转为 Markdown |
| `.xlsx` `.xls` | 调用 markitdown 转为 Markdown |
| `.pptx` `.ppt` | 调用 markitdown 转为 Markdown |
| `.html` `.htm` | 调用 markitdown 转为 Markdown |
| 其他格式 | 尝试 markitdown 转换；失败时按 UTF-8 文本写入 |

转换后的文件写入 `Vault/Projects/<项目名>/`（选择了 Vault 时）或 `.tmp/uploads/<项目名>/`（无 Vault 时），同时在数据库中注册为新项目。

#### 第三步：建立索引

上传完成后，在「项目初始化」页面：
1. 从下拉框选中刚注册的项目
2. 点击 **「初始化」** 按钮，系统对项目目录下的 Markdown 文件建立检索索引
3. 等待「初始化日志」中出现 ✓ 完成标记

#### 第四步：切换到分析

点击左侧导航栏的「项目审查」图标，选择项目，开始审查。

---

### 追加文件到已有项目

选中一个已注册项目后，项目详情卡片右上角会出现 **「➕ 追加文件」** 按钮：

1. 点击「➕ 追加文件」
2. 弹窗直接进入 Step 2（文件选择），无需再选 Vault
3. 选择要追加的文件，点击「上传并追加」
4. 追加成功后返回初始化页，重新点击「初始化」建立增量索引

---

### 选择目录注册（本机部署）

> 适用于直接在本机运行后端、不使用 Docker 的场景。

点击 **「选择目录注册」**（灰色次要按钮），通过浏览器原生目录选择器选择项目目录。该目录会被直接注册为项目根路径，不做文件复制或转换。

**注意：** Docker 部署时此方式只能选择容器内的目录（无实际意义）。推荐改用「上传文件初始化」。

---

### 删除项目

选中项目后，若该项目**无已完成的审查记录**，详情卡片右上角会显示 **「删除」** 按钮（红色）：

- 删除会同时清除：项目注册记录、所有分块/索引、所有会话及输出
- **不会**删除磁盘上的文档文件（Vault 内的原始文档保留）
- 若项目已有审查记录，按钮变为灰色「有记录」标签，无法直接删除（保护数据完整性）

---

### 关联 Vault（已注册项目）

选中项目后，若系统中存在已注册的 Vault，项目详情卡片底部会显示 **「📒 Vault：」** 下拉框：

- 可将已有项目关联到一个 Vault（元数据关联，不移动文件）
- 勾选 **「解析 Wikilinks」** 后，索引时会展开 `[[链接]]` 语法，将被引用文件一起纳入语料

---

## 项目审查

### 文档格式要求

系统以项目目录中的 **Markdown（.md）** 文件作为审查语料。通过「上传文件初始化」时自动转换（见「项目初始化」章节），无需手动预处理。若需手动转换，可使用以下工具：

| 工具 | 适用场景 |
| --- | --- |
| [Pandoc](https://pandoc.org/) | 命令行批量转换，支持 docx / odt / rst / latex → md |
| [markitdown](https://github.com/microsoft/markitdown) | Python 工具，批量将 Office 文档 / PDF 转为 Markdown |
| [Obsidian](https://obsidian.md/)（+ 导出插件） | Word / PDF → Markdown，笔记型内容 |
| [Typora](https://typora.io/) | 编辑并导出 Word / PDF 为 Markdown |

---

### 操作流程

#### 第一步：选择项目

1. 点击左侧**审计图标**切换到「项目审查」界面。
2. 在底部输入框左侧点击 **「+」按钮**，从弹出的下拉菜单中选择已初始化的项目。
3. 所选项目名称会显示在输入框旁边作为当前上下文标记。

> 若项目列表为空，先点击「初始化新项目」跳转到「项目初始化」页注册并索引项目。

#### 第二步：执行审查

在输入框中描述审查目标，按 Enter 或点击发送按钮开始审查，系统自动编排分析流程。

#### 第三步：查看里程碑进度

分析过程中，流程区依次显示以下里程碑：

| 里程碑 | 说明 |
| --- | --- |
| **问题分析** | 理解用户输入，识别文档类型与审查意图 |
| **评审策略** | 从活动技能包自动路由适合的关注点组合 |
| **解析文档** | 召回相关文档分块，构建上下文 |
| **思考分析** | LLM 推理，末尾显示「【加载的记忆】」命中片段列表 |
| **呈现结果** | 最终结论输出，包含片段引用与导出入口 |

#### 第四步：多轮追问

分析完成后，在底部输入框继续提问即可发起追问，上下文自动延续。

#### 第五步：导出与知识归纳

- 点击结果区「**审查报告**」按钮，可下载 `.md` 格式审查报告。
- 点击结果区「**提取知识**」按钮（灯泡图标），切换到知识归纳模块，自动携带本次审查结论。

---

### 知识线索（Knowledge Clues）

每次项目审查结束后，后端自动识别结论中可泛化的知识点，生成**知识线索**，存储在后台审查队列中。

知识线索是 AI 的「原始素材」：在知识归纳阶段帮助 LLM 识别规则空白，尤其在选择「**发现规则盲点**」策略时，系统会对比现有规则库与知识线索，优先覆盖尚未转化为规则的模式。

---

### 记忆系统（Memory）

系统在每次分析时自动注入项目相关的记忆片段：

**存储位置**

```
~/.aika/memory/
├── user/                    # 用户级记忆（跨项目）
└── project/<project_id>/    # 项目级记忆
    ├── terms.md             # 项目术语表
    └── ...
```

**注入机制**：按关键词匹配打分，命中片段写入系统提示；「思考分析」末尾显示「【加载的记忆】」命中列表。

---

### 技能包与关注点

审查系统通过**技能包（Skill Package）**管理关注点：

- **默认路径**：`review_skill_packages/<包id>/`
- **切换技能包**：点击左侧**搜索图标（审查域设定）**
- **技能包内容**：`manifest.json`、`focus-points/focus-*.md`（v2 格式）、`review_domain.md`（v1 兼容）

系统分析时从当前活动包中自动路由适合的关注点组合，无需手动选择。

---

## 知识归纳

知识归纳模块将**专家经验转化为可入库的规则**。通过 AI 主导的对话，逐步澄清、提炼、审批知识条目。

### 模块入口与权限

点击左侧**灯泡图标（知识归纳）**进入模块。页面分为：

- **左侧**：会话历史侧边栏（收藏会话、近期会话、所有会话）
- **右侧**：归纳工作台（对话区 + 输入框）

点击左侧**图钉图标（待批准规则）**进入规则审批页面（独立于归纳工作台）。

完整的知识归纳与规则审批功能需要：

```bash
AIKA_USER_ROLE=senior_expert
```

未设置时，页面显示「权限不足」提示。详见[设置指南](#设置指南)中的环境变量说明。

---

### 归纳工作台

#### 新建会话

点击会话列表顶部的「**新会话**」按钮，开始一次新的归纳对话。

AI 开场白：「你好！今天想总结些什么经验？选一个方向，或者直接告诉我你想聊什么：」

随后展示四种**提取策略**供选择：

| 策略 | 内部标识 | 适用场景 | AI 行为 |
| --- | --- | --- | --- |
| **发现规则盲点** | gap_based | 想系统梳理现有规则空白 | 对比知识线索与现有规则库，识别尚未覆盖的模式 |
| **澄清模糊印象** | fuzzy_signal | 有模糊感觉但说不清楚 | 追问细节，将模糊印象转化为清晰规则 |
| **复盘具体案例** | critical_incident | 有具体案例或事故想提炼规律 | 基于事件细节追问，提炼可复用经验规则 |
| **挑战现有规则** | reverse_validation | 想确认/修正一条已有规则 | 提出质疑场景，专家确认或反驳，迭代规则边界 |

选择策略后，AI 开始主动提问，进入对话阶段。

#### 上传参考文档

在输入框工具栏点击 **「+」按钮** → 选择「**上传参考文档**」，支持 `.md`、`.html`、`.docx`、`.pdf`、`.xlsx`、`.csv`、`.pptx` 等格式。文档加载后以标签形式显示在输入框上方，AI 将基于文档内容进行知识澄清。

#### 审查后提取

在项目审查结果页点击「**提取知识**」按钮，系统自动切换到知识归纳并进入**审查后提取**模式：

- AI 接收本次审查结论，识别可提炼的知识点
- 点击「**开始提取**」，AI 自动发起第一轮提问
- 支持多轮追问，提取完成后生成知识条目进入审批流程

#### 换个方向

对话进行中随时可点击输入框工具栏的「**换个方向**」按钮，返回策略选择界面，开始新一轮不同方向的提炼，历史对话仍保留在会话记录中。

---

### 知识条目（KI）

AI 每次生成知识条目，以**内联卡片**形式直接嵌入对话流中展示：

| 操作 | 说明 | 结果状态 |
| --- | --- | --- |
| **确认入库** | 采纳该条目 | `approved`，发送至「待批准规则」列表 |
| **修改** | 弹出编辑弹窗，修改标题/内容后确认 | `edited`，发送至「待批准规则」列表 |
| **不采纳** | 拒绝该条目 | `rejected`，不进入规则库 |

---

### 会话历史

每次归纳对话自动保存为一条会话记录：

| 操作 | 说明 |
| --- | --- |
| **收藏** | 将重要会话标记为收藏，显示在列表顶部 |
| **重命名** | 修改会话标题 |
| **继续** | 点击历史会话，恢复完整对话记录继续提炼 |
| **删除** | 永久删除会话记录 |
| **所有会话** | 点击底部「所有会话」，查看和搜索全部会话 |

---

### 待批准规则（Pending Rules）

点击左侧**图钉图标（待批准规则）**，进入规则审批页面。

所有由 AI 提取、等待人工确认的知识条目集中于此：

| 字段 | 说明 |
| --- | --- |
| **标题** | 规则名称 |
| **关注点** | 关联的关注点 ID |
| **内容摘要** | 规则正文摘要（展开行可查看完整内容） |
| **置信度** | `high` / `medium` / `low`（对应绿 / 橙 / 红标签） |
| **冲突** | 与现有规则的相似度冲突检测（悬停查看详情） |

**操作**

| 操作 | 说明 |
| --- | --- |
| **批准** | 将规则写入当前活动技能包（新关注点或现有关注点版本升级） |
| **拒绝** | 填写拒绝原因后标记 `rejected`，不写入规则库 |
| **展开行** | 查看完整内容与适用范围（`scope_note`） |

---

### 后端 API 参考（知识归纳）

| 端点 | 方法 | 说明 |
| --- | --- | --- |
| `/api/v1/extraction/active` | `POST`（SSE） | 主动提取会话流 |
| `/api/v1/extraction/doc` | `POST`（SSE） | 文档澄清会话流 |
| `/api/v1/extraction/review` | `POST`（SSE） | 审查后提取会话流 |
| `/api/v1/extraction/ki/{kid}` | `PATCH` | 更新知识条目（标题、内容、状态） |
| `/api/v1/extraction/ki/{kid}/submit` | `POST` | 提交知识条目到「待批准规则」 |
| `/api/v1/extraction/strategies` | `GET/POST` | 提取策略库增删查 |
| `/api/v1/extraction/strategies/bootstrap` | `POST` | 初始化内置种子策略（幂等） |
| `/api/v1/pending-rules` | `GET` | 获取待批准规则列表 |
| `/api/v1/pending-rules/{kid}/approve` | `POST` | 批准规则，写入技能包 |
| `/api/v1/pending-rules/{kid}/reject` | `POST` | 拒绝规则（需填原因） |
| `/api/v1/review-queue` | `GET` | 获取知识线索队列列表 |
| `/api/v1/review-queue/{id}` | `PATCH/DELETE` | 更新 / 删除知识线索 |

**SSE 事件类型**

| 事件 | 说明 |
| --- | --- |
| `status` | 阶段进度文字 |
| `text` | LLM 流式输出片段 |
| `ki` | 新知识条目（含 kid 和完整数据） |
| `clarify` | LLM 要求澄清，含建议问题列表 |
| `final` | 轮次结束，含满意度分数 |

---

## 审查域说明

审查域（Review Domain）由**技能包（Skill Package）**承载，定义了系统分析时使用的关注点和组合预设。

### 进入审查域设定

点击左侧**搜索图标（审查域设定）**，可：
- 查看当前活动技能包路径与关注点列表
- 编辑关注点 Prompt 内容
- 查看和新增组合预设
- 导入审查域文件（`.md` 格式，自动校验并写回活动包）

---

### 技能包目录结构

```
review_skill_packages/
└── <package-id>/
    ├── manifest.json          # 包元数据
    ├── focus-points/          # v2 格式关注点（推荐）
    │   ├── focus-req.md
    │   ├── focus-risk.md
    │   └── ...
    └── review_domain.md       # v1 格式（兼容旧版）+ 组合预设
```

### manifest.json 格式

```json
{
  "id": "package-general",
  "name": "通用审查包",
  "version": "1.0.0",
  "description": "适用于通用项目审查的关注点集合"
}
```

### 关注点文件格式（v2，推荐）

```markdown
---
schema: aika-focus/1
id: req
name: 需求完整性
version: 2
applicable_when:
  doc_types: [blueprint, requirement]
scope_note: "仅适用于项目前期蓝图与需求类文档"
not_applicable_when:
  doc_types: [progress_report, acceptance]
---

（关注点 Prompt 正文）
```

**YAML Front Matter 字段说明**

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `schema` | 是 | 固定为 `aika-focus/1` |
| `id` | 是 | 关注点唯一标识，与文件名中的 `<id>` 对应 |
| `name` | 是 | 展示名称 |
| `version` | 否 | 版本号，批准新规则时自动递增 |
| `applicable_when` | 否 | 适用条件 |
| `scope_note` | 否 | 适用范围文字说明 |
| `not_applicable_when` | 否 | 明确不适用的场景 |

**常见关注点 ID 参考**

| ID | 说明 |
| --- | --- |
| `req` | 需求完整性 |
| `risk` | 风险识别 |
| `integration` | 集成与接口 |
| `scope` | 范围与边界 |
| `progress` | 进度与计划 |
| `quality` | 质量与测试 |
| `acceptance` | 验收标准 |
| `data` | 数据与治理 |

### 关注点格式（v1，兼容）

在 `review_domain.md` 中用三级标题定义：

```markdown
### focus:req | 需求完整性

（关注点 Prompt 正文）
```

### 组合预设

在 `review_domain.md` 中添加包含「组合」+「建议/预设」字样的**二级标题**，并紧跟**五列**表格：

```markdown
## 组合使用建议

| 评审节点 | 推荐组合的关注点 | 审查角色 | 审查目标与原则 | 输出要求 |
| --- | --- | --- | --- | --- |
| 蓝图评审 | `focus:req` + `focus:integration` | 架构师 | 检查需求与接口完整性 | 输出风险清单 |
```

关注点引用格式为 `` `focus:<id>` ``，与 `focus-points/focus-<id>.md` 的 `id` 字段完全一致。

### 批准规则写入位置

规则审批后自动写入当前活动技能包的 `focus-points/` 目录（若关注点已存在则版本号自动递增）：

```
review_skill_packages/<活动包id>/focus-points/focus-<id>.md
```

---

## 设置指南

点击底部**用户头像** → 弹出菜单 → 选择「**设置**」，弹窗内分两个页签：

> **注意**：审查域（关注点与组合预设）管理已移至左侧导航的「**审查域设定**」页（搜索图标），不在设置弹窗中。

### 文档设置

| 选项 | 说明 |
| --- | --- |
| **分块上限（chunk limit）** | 单次分析送入模型的片段数量上限（默认 30）。越大上下文越长，费用与耗时也越高 |
| **分块策略** | **空行分块**：按空行切段再按字数装箱；**标题与结构感知**：按 `#` / `##` 等标题与代码围栏分节再装箱（`.html` 文件仍用空行分块） |
| **索引模式** | **增量**：只解析新文件与内容有变化的文件（默认，较快）；**全量**：清空索引后全量重建 |
| **不解析图片** | 索引阶段是否跳过 HTML 文档内图片内容 |

> 修改分块策略后，必须重新执行「索引与分块」，否则分析基于旧结果；界面会提示「需要重新索引」。

### 模型设置

文本大模型与视觉大模型（VL）可分别配置：

| 字段 | 说明 |
| --- | --- |
| **Provider** | LLM 提供商（目前支持 `openai_compatible`） |
| **Base URL** | API 接入点，如 `https://api.openai.com/v1` 或自托管地址 |
| **API Key** | 对应 Provider 的鉴权 Key |
| **Model** | 模型标识符，如 `gpt-4o`、`qwen3` |

> 配置保存后立即生效，无需重启服务。

### 审查域设定

点击左侧**搜索图标（审查域设定）**进入，包含：

| 子功能 | 说明 |
| --- | --- |
| **当前活动包** | 显示活动技能包 ID 与路径，切换技能包 |
| **关注点列表** | 浏览、展开和编辑当前包中的关注点 Prompt |
| **组合预设** | 查看和新增审查节点的关注点组合建议 |
| **导入审查域** | 上传 `.md` 文件并校验格式，通过后写回当前活动包 |

### 环境变量

| 变量 | 默认值 | 说明 |
| --- | --- | --- |
| `AIKA_USER_ROLE` | `consultant` | `senior_expert` 可访问完整知识归纳功能 |
| `AIKA_REPO_ROOT` | 代码仓库根目录 | 覆盖仓库根路径（影响技能包、记忆、帮助文档查找路径） |
| `AIKA_REVIEW_SKILL_PACKAGES_ROOT` | `$AIKA_REPO_ROOT/review_skill_packages` | 覆盖技能包根目录 |
| `AIKA_LLM_PROVIDER` | `openai_compatible` | LLM 提供商 |
| `AIKA_LLM_BASE_URL` | — | 文本大模型 API 接入点 |
| `AIKA_LLM_API_KEY` | — | 文本大模型 API Key |
| `AIKA_LLM_MODEL` | — | 文本大模型标识符 |
| `AIKA_WEB_HOST` | `0.0.0.0` | 服务监听地址 |
| `AIKA_WEB_PORT` | `8765` | 服务监听端口（Docker 默认） |
| `AIKA_CHUNK_STRATEGY` | `blank` | 分块策略（`blank` / `structured`） |

**`.env` 配置示例：**

```bash
AIKA_USER_ROLE=senior_expert
AIKA_LLM_PROVIDER=openai_compatible
AIKA_LLM_BASE_URL=https://api.openai.com/v1
AIKA_LLM_API_KEY=sk-xxxxxxxxxxxxxxxx
AIKA_LLM_MODEL=gpt-4o
AIKA_WEB_PORT=8765
```

### Docker 部署要点

- 挂载 Obsidian Vault 目录：`docker-compose.yml` 中添加 `- /host/vault:/vault:rw`，再在「Obsidian Vault」弹窗中填入 `/vault`
- 通过 `environment:` 设置环境变量
- 建议挂载持久卷（`ai-ka-data:/app/.tmp`）保留数据库与记忆文件

详细部署步骤见仓库内 `docs/docker-deploy.md`。

---

## 常见问题

### 帮助文档无法加载（helpme.md not found）

后端在以下位置依次查找帮助文档：
1. `$AIKA_REPO_ROOT/helpme.md`
2. `$AIKA_REPO_ROOT/docs/helpme.md`

若返回 404，请确认 `AIKA_REPO_ROOT` 是否正确指向仓库根目录，且 `helpme.md` 存在。Docker 部署时需重新构建镜像（`docker build -t ai-ka:latest .`）。

---

### 报错 `404 page not found` 或 API 调用失败

1. 检查**设置 → 模型**中的 Base URL 和 API Key 是否正确
2. 确认 Base URL 末尾格式（通常需要 `/v1`，不带斜杠结尾）
3. 查看后端进程标准输出获取详细错误信息

---

### 如何选择项目？界面上找不到项目选择器

在「项目审查」界面，项目选择入口在底部**输入框左侧的「+」按钮**内，点击后从下拉菜单选择已初始化项目。若项目列表为空，先去「项目初始化」页面注册并索引项目。

---

### 上传时提示「conversion failed」

markitdown 转换失败常见原因：
- `.pdf` 需要 `pdfminer.six` 依赖（Docker 镜像中已包含，本机安装需手动安装）
- `.docx` 加密保护的文件无法读取
- 图片扫描件（无文字层）的 PDF 需要 OCR（markitdown 不含 OCR）

处理方式：先手动将文件另存为 `.txt` 或导出为 `.md`，再上传纯文本版本。

---

### Docker 下「选择目录注册」选不到宿主机目录

Docker 容器与宿主机文件系统隔离，推荐改用「📤 上传文件初始化」，通过浏览器文件上传接口将宿主机文件传入容器。

若需挂载宿主机 Obsidian Vault，在 `docker-compose.yml` 中添加：

```yaml
volumes:
  - /path/to/your/vault:/vault:rw
```

然后在「Obsidian Vault」弹窗中填入容器内路径 `/vault`。

---

### 文档索引完成但分析结果为空或片段很少

可能原因：分块数量少、分块策略与文档结构不匹配、chunk 上限过低。

建议：切换分块策略 → 重新索引 → 调高 chunk 上限 → 重新分析。

---

### 导入审查域后没有「组合预设」

请确认文件中：
1. 有包含「组合」+「建议/预设」字样的**二级标题**（如 `## 组合使用建议`）
2. 紧跟**五列** Markdown 表格
3. 关注点引用格式为 `` `focus:<id>` ``，与 `focus-points/focus-<id>.md` 的 `id` 字段完全一致

---

### 知识归纳页面显示「权限不足」

需要设置 `AIKA_USER_ROLE=senior_expert` 后**重启后端服务**：

```bash
export AIKA_USER_ROLE=senior_expert
```

或在 `.env` 文件中添加后重启 Docker 容器。

---

### 批准规则后在审查域中找不到

规则写入**当前活动技能包**的 `focus-points/` 目录。在**审查域设定**页面刷新，或直接检查 `review_skill_packages/<活动包id>/focus-points/` 目录。若关注点已存在，版本号自动递增。

---

### 设置里找不到审查域配置

审查域（关注点与组合预设）已从「设置」弹窗**独立为左侧导航的「审查域设定」页**（搜索图标）。设置弹窗仅保留「文档」分块与「模型」配置。

---

### 修改分块策略后分析结果没变

修改分块策略后，必须在**设置 → 文档**中触发重新索引（界面会出现提示），否则分析仍基于旧分块结果。

---

### 冲突检测误报

冲突检测基于文本相似度启发式规则，可能出现误报：
- **直接批准**：忽略冲突提示
- **调整适用范围**：修改两条规则的 `applicable_when` / `not_applicable_when` 字段，使适用场景明确区分

---

### 策略库 Bootstrap 需要运行几次

只需运行一次（幂等）：

```bash
curl -X POST http://localhost:8765/api/v1/extraction/strategies/bootstrap
```

已存在的种子策略不会重复插入。若策略库被意外清空，重新调用即可。
