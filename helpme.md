# AI-KA 使用帮助

## 功能简介

AI-KA（ProjectLens）是一套**通用业务文档审查 + 知识沉淀**平台，面向咨询顾问、技术评审专家和知识管理团队，将项目文档审查与经验知识提炼融为一体。

### 两大核心模块

**项目审查模块（Review）**

- 基于项目目录中已有的 **Markdown / HTML** 文件构建可检索语料库
- 结合**审查技能包**中定义的关注点（Focus Points）与 Prompt 对文档做结构化分析
- 流式输出分析里程碑与结论，支持多轮追问
- 自动注入项目记忆（术语、偏好、历史信息），在「思考分析」段显示命中片段
- 审查结束后 LLM 自动提取可泛化的行业/方法论知识到**审查队列**

**知识提取模块（Extraction）**

- **提取工作台**：三种开始方式（审查队列条目 / 自由提取 / 文档澄清），AI 主动提问
- **待批准规则**：人工审批后写入活动技能包，形成可复用规则
- **内联知识条目审核**：提取对话过程中实时审核 LLM 生成的知识条目
- **场边教练（Coach Agent）**：每 5 轮对话触发，向操作员提供提问策略建议
- **自学习飞轮**：策略库 + 元反思机制持续优化 LLM 提问策略

### 典型用途

| 场景 | 模块 |
| --- | --- |
| 蓝图 / 需求 / 上线前快速文档审查 | 项目审查 |
| 复盘阶段提炼可复用经验 | 知识提取（审查后提取） |
| 积累行业最佳实践 | 知识提取（自由提取） |
| 定期更新规则库 | 知识提取（规则差距策略） |
| 文档知识澄清与结构化 | 知识提取（文档澄清） |

### 界面布局

左侧导航栏提供快速入口：

| 图标 | 功能 |
| --- | --- |
| 文件夹图标 | 项目初始化 |
| 对话气泡图标 | 项目审查（分析） |
| 列表图标 | 审查队列 |
| 灯泡图标 | 知识提取 |
| 时钟图标 | 会话历史 |
| 齿轮图标 | 系统设置 |
| 问号图标 | 帮助文档（本页） |

---

## 项目初始化

项目初始化是审查的第一步：将项目文档上传到系统、写入 Obsidian Vault、建立检索索引，之后才能在「分析」页发起审查。

### 推荐工作流（Vault 优先）

```
配置 Obsidian Vault → 上传文件初始化（自动转换）→ 建立索引 → 切换到分析
```

#### 第一步：配置 Obsidian Vault（强烈建议）

点击「项目初始化」页面顶部的 **「Obsidian Vault」** 按钮，注册一个本地 Obsidian Vault 目录。

> 若未配置 Vault，页面顶部会显示黄色警告横幅：文件将写入后端临时目录（`<repo>/.tmp/uploads/<项目名>/`），重新部署容器后会丢失。配置 Vault 后，文档持久化到 Vault 的 `Projects/<项目名>/` 子目录，Obsidian 也能直接打开编辑。

**Vault 角色说明：**

| 角色 | 用途 |
| --- | --- |
| `project`（项目文档）| 存放项目文档，用于项目审查语料 |
| `knowledge`（知识库）| 存放知识提取结果，用于知识积累 |
| `both`（两用）| 同时承担项目文档与知识库 |

**注册方式：**
- **自动发现**：弹窗中点击「扫描本机 Vault」，系统会扫描 `~/Documents/` 等常见目录，发现包含 `.obsidian/` 文件夹的目录
- **手动注册**：在输入框填入 Vault 目录的**绝对路径**，选择角色，点击「注册」

注册成功后，Vault 下会自动创建 `Projects/` 和 `_aika/` 子目录。

#### 第二步：上传文件初始化项目

点击 **「📤 上传文件初始化」** 按钮，进入两步引导弹窗：

**Step 1 — 选择目标 Vault**（仅 init 模式）
- 从下拉框中选择已注册的 Vault（仅显示角色为 `project` 或 `both` 的 Vault）
- 若跳过选择，文件写入后端临时目录（不推荐生产使用）
- 若尚未注册任何 Vault，点击弹窗内的「去配置」按钮先完成注册

**Step 2 — 选择文件**
- **「📁 选择文件夹」**：选择整个项目文件夹，保留目录层级结构
- **「🗂 选择多个文件」**：手动选择多个文件，平铺写入项目根目录
- 选完后弹窗显示文件类型摘要（直接写入 / 自动转换 / 未知格式）
- 填写**项目名称**（从文件夹名自动识别，可手动修改）
- 点击「上传并初始化」

**支持的文件格式：**

| 格式 | 处理方式 |
| --- | --- |
| `.md` `.txt` `.markdown` | 直接写入，不做转换 |
| `.docx` `.doc` | 调用 markitdown 转为 Markdown |
| `.pdf` | 调用 markitdown 转为 Markdown |
| `.xlsx` `.xls` | 调用 markitdown 转为 Markdown |
| `.pptx` `.ppt` | 调用 markitdown 转为 Markdown |
| `.html` `.htm` | 调用 markitdown 转为 Markdown |
| 其他格式 | 尝试 markitdown 转换；失败时按 UTF-8 文本写入 |

转换后的文件写入 `Vault/Projects/<项目名>/`（选择了 Vault 时）或 `.tmp/uploads/<项目名>/`（无 Vault 时），同时在数据库中注册为新项目。

#### 第三步：建立索引

上传完成后，在「项目初始化」页面：
1. 从下拉框选中刚注册的项目
2. 点击 **「初始化」** 按钮，系统对项目目录下的 Markdown 文件建立检索索引
3. 等待「初始化日志」中出现 ✓ 完成标记

#### 第四步：切换到分析

点击左侧导航栏的「分析」图标，选择项目，开始项目审查。

---

### 追加文件到已有项目

选中一个已注册项目后，项目详情卡片右上角会出现 **「➕ 追加文件」** 按钮：

1. 点击「➕ 追加文件」
2. 弹窗直接进入 Step 2（文件选择），无需再选 Vault
3. 选择要追加的文件，点击「上传并追加」
4. 追加成功后返回初始化页，重新点击「初始化」建立增量索引

---

### 选择目录注册（本机部署）

> 适用于直接在本机运行后端、不使用 Docker 的场景。

点击 **「选择目录注册」**（灰色次要按钮），通过浏览器原生目录选择器选择项目目录。该目录会被直接注册为项目根路径，不做文件复制或转换。

**注意：** Docker 部署时此方式只能选择容器内的目录（无实际意义）。推荐改用「上传文件初始化」。

---

### 删除项目

选中项目后，若该项目**无已完成的审查记录**，详情卡片右上角会显示 **「删除」** 按钮（红色）：

- 删除会同时清除：项目注册记录、所有分块/索引、所有会话及输出
- **不会**删除磁盘上的文档文件（Vault 内的原始文档保留）
- 若项目已有审查记录，按钮变为灰色「有记录」标签，无法直接删除（保护数据完整性）

---

### 关联 Vault（已注册项目）

选中项目后，若系统中存在已注册的 Vault，项目详情卡片底部会显示 **「📒 Vault：」** 下拉框：

- 可将已有项目关联到一个 Vault（元数据关联，不移动文件）
- 勾选 **「解析 Wikilinks」** 后，索引时会展开 `[[链接]]` 语法，将被引用文件一起纳入语料

---

## 项目审查

### 文档格式要求

系统以项目目录中的 **Markdown（.md）** 文件作为审查语料。上传时自动转换（见「项目初始化」章节），无需手动预处理。若需手动转换，可使用以下工具：

| 工具 | 适用场景 |
| --- | --- |
| [Pandoc](https://pandoc.org/) | 命令行批量转换，支持 docx / odt / rst / latex → md |
| [markitdown](https://github.com/microsoft/markitdown) | Python 工具，批量将 Office 文档 / PDF 转为 Markdown |
| [Obsidian](https://obsidian.md/)（+ 导出插件） | Word / PDF → Markdown，笔记型内容 |
| [Typora](https://typora.io/) | 编辑并导出 Word / PDF 为 Markdown |
| docs2md（内置） | 后端集成的转换工具，可通过 CLI 调用 |

转换完成后，将 `.md` / `.html` 文件放入项目目录，再执行「项目初始化」建立索引。

---

### 操作流程

#### 第一步：项目初始化

1. 点击左侧**文件夹图标**进入「项目初始化」页面。
2. 在路径输入框填写**后端进程可见的绝对路径**（本机运行时可使用目录选择器；Docker 部署时填写容器内已挂载路径）。
3. 点击「注册项目」，系统自动扫描目录中的 `.md` / `.html` 文件并建立分块索引。
4. 初始化完成后，项目出现在项目列表中，显示文档数量与分块数量。

> **注意**：修改分块策略后，需重新执行「索引与分块」才能生效，否则分析仍基于旧分块结果。

#### 第二步：执行审查

1. 点击左侧**对话气泡图标**切换到「分析」视图。
2. 从顶部下拉框选择已初始化的项目。
3. 在输入框描述审查目标（可直接发送，也可附加补充说明）。
4. 点击发送或按 Enter，系统自动开始编排审查流程。

#### 第三步：查看里程碑进度

分析过程中，流程区依次显示以下里程碑：

| 里程碑 | 说明 |
| --- | --- |
| **问题分析** | 理解用户输入，识别文档类型与审查意图 |
| **评审策略** | 从活动技能包自动路由适合的关注点组合 |
| **解析文档** | 召回相关文档分块，构建上下文 |
| **思考分析** | LLM 推理，末尾显示「【加载的记忆】」命中片段列表 |
| **呈现结果** | 最终结论输出，包含片段引用与导出入口 |

#### 第四步：多轮追问

- 分析完成后，在底部输入框继续提问即可发起追问。
- 每次追问同样经历里程碑流程，上下文自动延续。
- 追问时可输入更具体的子问题，引导系统深入某个关注点。

#### 第五步：导出结果

- 审查结论可从结果区点击「审查报告」导出 `.docx` 或 `.md` 格式。
- 片段与来源索引表（文件路径 + 行号范围）随结论一并输出。

---

### 审查队列（Review Queue）

审查队列是项目审查与知识提取之间的**缓冲层**，自动暂存来自审查结论的泛化知识线索。

**入口**：点击左侧**列表图标**，右侧抽屉展开审查队列。

**线索来源**

每次项目审查结束后，LLM 自动识别结论中可泛化的知识点并写入队列，每条线索包含：

- 关联关注点 ID
- 建议摘要
- 来源（项目 ID / 会话 ID）
- 重复次数（同一建议多次出现时自动合并计数）
- 信任等级（资深顾问审查产生的线索为「中信任」，普通顾问为「低信任」）

**查看与操作**

- 列表按**重复次数降序**排列，优先展示高频线索
- 每条线索可执行以下操作：
  - **归档**：暂时忽略，不影响后续知识提取
  - **开始提取**：切换到知识提取工作台，自动携带该条目，进入「审查后提取」流程
  - **更改状态**：`pending_review` / `in_review` / `approved` / `rejected` / `archived`
  - **删除**：永久移除该条目
- 左侧导航列表图标上的**角标**显示当前待审查条目数量

---

### 记忆系统（Memory）

系统会在每次分析时自动注入项目相关的记忆片段，使 LLM 能够利用历史积累的项目信息。

**存储位置**

```
~/.aika/memory/
├── user/                    # 用户级记忆（跨项目）
│   └── expert_profile.md    # 专家画像（知识提取模块生成）
└── project/<project_id>/    # 项目级记忆
    ├── terms.md             # 项目术语表
    └── ...                  # 其他项目相关记忆
```

**注入机制**

- 分析时按关键词匹配打分，命中片段写入系统提示
- 「思考分析」里程碑末尾追加「【加载的记忆】」命中片段列表

**管理记忆**

- 在**设置 → 文档**中可查看和清除当前项目的记忆
- 删除对应 `.md` 文件后重启服务也可清除

---

### 技能包与关注点

审查系统通过**技能包（Skill Package）**管理关注点。

- **默认路径**：`review_skill_packages/<包id>/`
- **切换技能包**：在**设置 → 审查域**中选择当前活动包
- **技能包内容**：
  - `manifest.json`：包的元数据（id、名称、版本等）
  - `focus-points/focus-*.md`：v2 格式关注点文件（推荐）
  - `review_domain.md`：v1 格式（兼容旧版）

系统分析时会从当前活动包中自动路由适合的关注点组合，无需手动选择。

---

## 知识提取

### 权限要求

完整知识提取功能（主动提取、文档上传、待批准规则审批）需要环境变量：

```bash
AIKA_USER_ROLE=senior_expert
```

未设置时，页面显示「权限不足」提示。详见[设置指南](#设置指南)中的环境变量说明。

---

### 模块入口

点击左侧**灯泡图标**切换到「知识提取」模式，页面顶部两个标签页：

| 标签 | 功能 |
| --- | --- |
| **提取工作台** | 三种开始方式的统一会话界面 |
| **待批准规则** | 审批 LLM 提取的知识条目，批准后写入活动技能包 |

---

### 专家画像（首次进入）

**首次进入知识提取**时（专家画像尚未建立），系统启动 AI 引导的三步入职访谈：

**Step 1 — 专业领域**
- 从动态生成的领域列表中**多选**擅长方向
- 领域选项根据当前审查队列中的关注点分布动态生成（与队列内容相关的领域优先显示）
- 至少选择一个领域才能继续

**Step 2 — 工作背景（可选）**
- 描述工作年限、典型项目类型、常遇挑战等
- 供 LLM 优化后续提问角度，可跳过

**Step 3 — 确认**
- 预览 AI 生成的画像摘要
- 点击「保存并开始提取」完成初始化

画像保存至 `~/.aika/memory/user/expert_profile.md`，后续进入知识提取将跳过访谈，直接进入提取工作台。

> 如需修改画像，点击工作台右上角「专家画像」按钮 → 编辑领域并保存；或删除 `expert_profile.md` 文件后刷新页面重新触发访谈。

---

### 提取工作台

提取工作台有**三种开始方式**，通过顶部分段选择器切换：

#### 方式一：从审查队列开始

适用于针对已有知识线索深入提炼。

1. 在分段选择器中选择「从审查队列」
2. 从下拉框选择一条审查队列条目
3. 选择提取策略（见下方策略说明）
4. 点击「开始」，LLM 自动基于队列条目内容发起第一轮提问

#### 方式二：自由提取

适用于主动提炼某个领域经验，无需先完成项目审查。

1. 在分段选择器中选择「自由提取」
2. 选择提取策略
3. 在文本框中简要描述提取主题（可选）
4. 点击「开始」

#### 方式三：文档澄清

适用于上传已有规则文档，对其内容进行结构化知识澄清。

1. 在分段选择器中选择「文档澄清」
2. 拖拽或点击上传 `.md` / `.html` 文件
3. LLM 读取文档内容，基于现有规则库做知识澄清，进入多轮对话

---

### 四种提取策略

| 策略 | 适用场景 | LLM 行为 |
| --- | --- | --- |
| **规则差距（gap_based）** | 想系统梳理现有规则空白 | 对比审查队列与当前规则库，识别未覆盖模式，优先覆盖高优先级队列项 |
| **模糊信号（fuzzy_signal）** | 有模糊感觉但说不清楚 | 追问细节，帮你把模糊印象转化为清晰规则；自动检测「通常」「要看情况」等模糊词并追问例外 |
| **关键事件（critical_incident）** | 有具体案例或事故想提炼规律 | 基于事件细节追问，提炼可复用经验规则 |
| **反向验证（reverse_validation）** | 想确认/修正一条已有规则 | 提出质疑场景，专家确认或反驳，迭代规则边界 |

---

### 对话流程

1. LLM 根据所选策略主动提问（每轮只问 1 个主问题）。
2. 在输入框作答；AI 会自动检测模糊词并在下轮追问例外情况。
3. ChatArea 下方**满意度进度条**实时显示 LLM 对已提取知识的置信程度（0–1）：
   - **橙色（< 85%）**：LLM 认为知识尚未完整澄清，建议继续追问
   - **绿色（≥ 85%）**：满意度达标，LLM 自动生成知识条目
4. 满意度达标（或最多 5 轮后），知识条目以内联卡片形式出现在对话流中。
5. 对卡片执行**内联审核**（确认 / 修改 / 不采纳）后，通过的条目进入「待批准规则」。

---

### 内联知识条目审核

LLM 每次生成知识条目，以卡片形式直接嵌入对话流中展示：

| 操作 | 说明 | 结果状态 |
| --- | --- | --- |
| **确认入库** | 采纳该条目 | `approved`，写入「待批准规则」列表 |
| **修改** | 弹出编辑弹窗，修改标题/内容后确认 | `edited`，写入「待批准规则」列表 |
| **不采纳** | 拒绝该条目 | `rejected`，不进入规则库 |

---

### 场边教练（Coach Agent）

每隔 **5 轮对话**自动触发，对话区底部出现绿色教练提示条：

| 字段 | 说明 |
| --- | --- |
| **核心建议（whisper）** | 1–2 句，建议的下一轮提问方向 |
| **专家类型推断** | 如「偏执行派，不善理论化」 |
| **未覆盖领域** | 识别到的知识空白列表 |
| **对话势头** | 「停滞」/「减弱」时显示警告标签 |
| **风险标记（flag）** | 如「警惕：专家开始给教科书式答案」 |

**操作**
- 点击「**采纳建议**」：whisper 内容预填到输入框，可直接发送或修改
- 点击「**忽略**」：关闭提示，继续自主提问

> 教练提示**仅对操作员可见**，不注入 AI 上下文，不影响主对话流程。若教练分析失败，会静默跳过。

---

### 审查后提取（Post-Review Extraction）

在项目审查结果页点击「**提取知识**」，系统自动切换到知识提取模式并进入「审查后提取」状态：

- LLM 解析本次审查结论，识别可泛化的知识点
- 点击「开始提取」自动发起第一轮提问
- 支持多轮追问、内联知识条目卡片审核、教练提示
- 提取完成后进入「待批准规则」审批流程

---

### 待批准规则（Pending Rules）

所有由 LLM 提取、等待人工确认的知识条目集中在「**待批准规则**」标签页。

**操作**

| 操作 | 说明 |
| --- | --- |
| **批准** | 将规则写入活动技能包（新关注点或现有关注点版本升级） |
| **拒绝** | 填写拒绝原因后标记 `rejected`，不写入规则库 |
| **展开行** | 查看完整内容与适用范围 |

---

### 自学习飞轮（三层机制）

| 层级 | 时间尺度 | 实现 |
| --- | --- | --- |
| **Scale-1** | 会话内 | 专家建模协议（Expert Modeling Protocol）注入系统提示，动态调整问题角度 |
| **Scale-2A** | 每 5 轮 | 场边教练 Agent（Coach）分析对话历史给出建议，引导专家提供更高质量回答 |
| **Scale-2B** | 会话后 | 元反思（Meta-Reflection）复盘，更新策略有效性分数 |

策略库内置 12 条 Bootstrap 种子策略，首次部署调用 `POST /api/v1/extraction/strategies/bootstrap` 初始化（幂等，可多次调用）。

---

### API 端点参考（知识提取）

| 端点 | 方法 | 说明 |
| --- | --- | --- |
| `/api/v1/extraction/active` | `POST`（SSE） | 主动提取会话流 |
| `/api/v1/extraction/doc` | `POST`（SSE） | 文档澄清会话流 |
| `/api/v1/extraction/review` | `POST`（SSE） | 审查后提取会话流 |
| `/api/v1/extraction/ki/{kid}` | `PATCH` | 更新知识条目（标题、内容、状态） |
| `/api/v1/extraction/ki/{kid}/submit` | `POST` | 提交知识条目到「待批准规则」 |
| `/api/v1/extraction/meta-reflect` | `POST/GET` | 发起 / 查看会话后元反思 |
| `/api/v1/extraction/strategies` | `GET/POST` | 策略库增删查 |
| `/api/v1/extraction/strategies/bootstrap` | `POST` | 初始化 12 条种子策略（幂等） |
| `/api/v1/review-queue` | `GET` | 获取审查队列列表 |
| `/api/v1/review-queue/{id}` | `PATCH/DELETE` | 更新 / 删除队列条目 |

**SSE 事件类型**

| 事件 | 说明 |
| --- | --- |
| `status` | 阶段进度文字 |
| `text` | LLM 流式输出片段 |
| `ki` | 新知识条目（含 kid 和完整数据） |
| `coach_hint` | 场边教练建议（每 5 轮触发） |
| `clarify` | LLM 要求澄清，含建议问题列表 |
| `final` | 轮次结束，含满意度分数与 auto_advance 标志 |

---

## 审查域说明

审查域（Review Domain）由**技能包（Skill Package）**承载，定义了系统分析时使用的关注点和组合预设。

### 技能包目录结构

```
review_skill_packages/
└── <package-id>/
    ├── manifest.json          # 包元数据
    ├── focus-points/          # v2 格式关注点（推荐）
    │   ├── focus-req.md       # 需求完整性关注点
    │   ├── focus-risk.md      # 风险识别关注点
    │   └── ...
    └── review_domain.md       # v1 格式（兼容旧版），也用于存储组合预设
```

### manifest.json 格式

```json
{
  "id": "package-general",
  "name": "通用审查包",
  "version": "1.0.0",
  "description": "适用于通用项目审查的关注点集合"
}
```

### 关注点文件格式（v2，推荐）

每个关注点对应 `focus-points/` 目录下一个 `.md` 文件：

```markdown
---
schema: aika-focus/1
id: req
name: 需求完整性
version: 2
applicable_when:
  doc_types: [blueprint, requirement]
scope_note: "仅适用于项目前期蓝图与需求类文档"
not_applicable_when:
  doc_types: [progress_report, acceptance]
---

（关注点 Prompt 正文，描述 LLM 应如何审查该维度）
```

**YAML Front Matter 字段说明**

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `schema` | 是 | 固定为 `aika-focus/1` |
| `id` | 是 | 关注点唯一标识，与文件名中的 `<id>` 对应 |
| `name` | 是 | 展示名称 |
| `version` | 否 | 版本号，批准新规则时自动递增 |
| `applicable_when` | 否 | 适用条件（文档类型列表） |
| `scope_note` | 否 | 适用范围文字说明 |
| `not_applicable_when` | 否 | 明确不适用的场景 |

**常见关注点 ID 参考**

| ID | 说明 |
| --- | --- |
| `req` | 需求完整性 |
| `risk` | 风险识别 |
| `integration` | 集成与接口 |
| `scope` | 范围与边界 |
| `progress` | 进度与计划 |
| `quality` | 质量与测试 |
| `acceptance` | 验收标准 |
| `data` | 数据与治理 |

### 关注点文件格式（v1，兼容）

在 `review_domain.md` 中用三级标题定义：

```markdown
### focus:req | 需求完整性

（关注点 Prompt 正文）

### focus:risk | 风险识别

（关注点 Prompt 正文）
```

### 组合预设

在 `review_domain.md` 中添加**包含「组合」+「建议/预设」字样的二级标题**，并紧跟**五列**表格：

```markdown
## 组合使用建议

| 评审节点 | 推荐组合的关注点 | 审查角色 | 审查目标与原则 | 输出要求 |
| --- | --- | --- | --- | --- |
| 蓝图评审 | `focus:req` + `focus:integration` | 架构师 | 检查需求与接口完整性 | 输出风险清单 |
| 需求评审 | `focus:req` + `focus:risk` + `focus:scope` | 需求分析师 | 核查范围与风险 | 输出问题列表 |
| 上线前评审 | `focus:quality` + `focus:acceptance` | 测试负责人 | 核查质量与验收条件 | 输出 Go/No-Go 建议 |
```

推荐列中的关注点引用格式为 `` `focus:<id>` ``，与 `focus-points/focus-<id>.md` 的 `id` 字段完全一致。

### 管理技能包

**在 UI 中操作**

- **设置 → 审查域**：查看当前活动包路径，切换活动包
- **设置 → 审查域 → 关注点**：浏览、编辑关注点内容
- **设置 → 审查域 → 导入**：上传 `review_domain.md` 文件并写回当前活动包

**命令行操作**

```bash
# 列出所有技能包
python -m aika.cli skill list

# 激活指定技能包
python -m aika.cli skill activate <package-id>
```

**待批准规则写入位置**

规则审批后自动写入当前活动技能包的 `focus-points/` 目录（若关注点已存在则版本自动递增）。

---

## 设置指南

点击左侧**齿轮图标**进入系统设置，分三个页签：

### 文档设置

| 选项 | 说明 |
| --- | --- |
| **分块上限（chunk limit）** | 单次分析送入模型的片段数量上限（默认 30）。越大上下文越长，分析越深入，费用与耗时也越高 |
| **分块策略** | **空行分块**：按空行切段再按字数装箱（与旧版一致）；**标题与结构感知**：按 `#` / `##` 等标题与代码围栏分节再装箱（`.html` 文件仍用空行分块） |
| **不解析图片** | 索引阶段是否跳过 HTML 文档内图片内容（节省索引时间） |
| **重新索引** | 修改分块策略后，必须点击此按钮重新扫描，否则分析基于旧分块结果 |

> 修改分块策略后，界面会提示「需要重新索引」，点击确认后才会生效。

### 审查域设置

| 选项 | 说明 |
| --- | --- |
| **当前活动包** | 显示当前使用的技能包 ID 与路径，点击可切换 |
| **关注点列表** | 浏览当前包中的所有关注点，支持展开查看 Prompt 内容 |
| **编辑关注点** | 直接在 UI 中修改关注点 Prompt，保存后立即生效 |
| **导入审查域** | 上传 `.md` 文件并校验格式，通过后写回当前包的 `review_domain.md` |
| **组合预设** | 查看 `review_domain.md` 中定义的预设组合（供参考，分析时自动路由） |

### 模型设置

文本大模型与视觉大模型（VL）可分别配置：

| 字段 | 说明 |
| --- | --- |
| **Provider** | LLM 提供商（目前支持 `openai_compatible` 和 `mock`） |
| **Base URL** | API 接入点，如 `https://api.openai.com/v1` 或自托管地址 |
| **API Key** | 对应 Provider 的鉴权 Key |
| **模型名（Model）** | 模型标识符，如 `gpt-4o`、`claude-3-5-sonnet-20241022` |

> 配置保存后立即生效，无需重启服务。若配置错误，分析时后端日志会显示 API 调用错误。

### 环境变量

服务启动前可通过环境变量覆盖默认行为：

| 变量 | 默认值 | 说明 |
| --- | --- | --- |
| `AIKA_USER_ROLE` | `consultant` | `senior_expert` 可访问完整知识提取功能（主动提取、待批准规则审批） |
| `AIKA_REPO_ROOT` | 代码仓库根目录 | 覆盖仓库根路径（影响技能包、记忆、帮助文档查找路径） |
| `AIKA_REVIEW_SKILL_PACKAGES_ROOT` | `$AIKA_REPO_ROOT/review_skill_packages` | 覆盖技能包根目录 |
| `AIKA_LLM_PROVIDER` | `openai_compatible` | LLM 提供商，可选 `mock`（用于测试） |
| `AIKA_LLM_BASE_URL` | — | 文本大模型 API 接入点 |
| `AIKA_LLM_API_KEY` | — | 文本大模型 API Key |
| `AIKA_LLM_MODEL` | — | 文本大模型标识符 |
| `AIKA_WEB_HOST` | `0.0.0.0` | 服务监听地址 |
| `AIKA_WEB_PORT` | `8000` | 服务监听端口 |
| `DOCS2MD_PYTHON` | `python3` | 用于运行 docs2md 子进程的 Python 可执行文件路径 |
| `AIKA_CHUNK_STRATEGY` | `blank` | 分块策略（`blank` 空行分块 / `structured` 标题结构感知） |

**在 .env 文件中配置示例：**

```bash
AIKA_USER_ROLE=senior_expert
AIKA_LLM_PROVIDER=openai_compatible
AIKA_LLM_BASE_URL=https://api.openai.com/v1
AIKA_LLM_API_KEY=sk-xxxxxxxxxxxxxxxx
AIKA_LLM_MODEL=gpt-4o
AIKA_WEB_PORT=8000
```

### Docker 部署要点

- 挂载项目目录时，确保容器内路径与「项目初始化」中填写的路径一致
- 在 `docker-compose.yml` 中通过 `environment:` 设置上述环境变量
- 数据库文件（SQLite）和记忆文件默认存储在容器内；建议挂载持久卷以避免重启后丢失数据

详细部署步骤见仓库内 `docs/docker-deploy.md`。

---

## 常见问题

### 帮助文档无法加载（helpme.md not found）

后端在以下位置依次查找帮助文档：
1. `$AIKA_REPO_ROOT/helpme.md`
2. `$AIKA_REPO_ROOT/docs/helpme.md`

若两个路径均不存在，返回 404。请确认 `AIKA_REPO_ROOT` 环境变量是否正确指向仓库根目录，且 `helpme.md` 文件存在于其中。

---

### 报错 `404 page not found` 或 API 调用失败

多为 Base URL 与网关路径不一致，或 API Key 错误。请：
1. 检查**设置 → 模型**中的 Base URL 和 API Key 是否正确
2. 确认 Base URL 末尾格式（通常需要 `/v1`，不带斜杠结尾）
3. 查看后端日志（`web/backend/main.py` 所在进程的标准输出）获取详细错误信息

---

### 文档索引完成但分析结果为空或片段很少

可能原因：
- 文档内容过于简短，分块数量少
- 分块策略与文档结构不匹配（如 `.html` 文件使用标题感知模式时效果不佳）
- chunk 上限设置过低

建议尝试：切换分块策略 → 重新索引 → 调高 chunk 上限 → 重新分析。

---

### 导入审查域后没有「组合预设」

请确认文件中：
1. 有包含「组合」+「建议/预设」字样的**二级标题**（`## 组合使用建议`）
2. 紧跟**五列** Markdown 表格（`评审节点 | 推荐组合的关注点 | 审查角色 | 审查目标与原则 | 输出要求`）
3. 关注点引用格式为 `` `focus:<id>` ``，与 `focus-points/focus-<id>.md` 的 `id` 字段完全一致

---

### 知识提取页面显示「权限不足」

需要设置 `AIKA_USER_ROLE=senior_expert` 后**重启后端服务**。

```bash
export AIKA_USER_ROLE=senior_expert
# 重启后端
```

或在 `.env` 文件中添加后重启 Docker 容器。

---

### 审查队列没有内容

审查队列来自项目审查后的自动提取。请先完成至少一次完整的项目审查（需达到「呈现结果」里程碑）。若审查完成后队列仍为空，检查后端日志中是否有提取错误（关键字：`review_queue` 或 `extract_knowledge`）。

---

### 首次进入知识提取没有看到访谈流程

如果专家画像已存在（`~/.aika/memory/user/expert_profile.md` 文件存在，或后端数据库中记录非空），系统会跳过访谈直接进入工作台。

重新触发访谈的方法：
- 点击工作台右上角「专家画像」→ 清空所有领域并保存
- 或删除 `~/.aika/memory/user/expert_profile.md` 文件后刷新页面

---

### 场边教练不出现

教练每 **5 轮**触发一次。若 5 轮后仍未出现：
- 检查后端日志中的 `coach` 相关错误（教练分析失败会静默跳过，不影响主流程）
- 确认当前对话轮次计数是否正确（会话内轮次从 1 开始）

---

### 满意度进度条显示 0% 但已回答多轮

满意度由 LLM 内部评估，取决于：
- 规则表述是否清晰（有具体条件、边界）
- 适用范围是否明确
- 是否举出了例外或反例
- LLM 对置信度的判断

建议：更具体地描述场景、举出反例或边界情况，满意度会上升。换用「关键事件」策略并提供具体案例也会有帮助。

---

### 批准规则后在审查域中找不到

规则写入**当前活动技能包**的 `focus-points/` 目录：
- 路径：`review_skill_packages/<活动包id>/focus-points/focus-<id>.md`
- 若关注点已存在，版本号自动递增
- 在**设置 → 审查域**刷新页面即可看到更新后的关注点

---

### 冲突检测误报

冲突检测基于文本相似度启发式规则，可能出现误报。处理方式：
- **直接批准**：忽略冲突提示，批准规则
- **调整适用范围**：修改两条规则的 `applicable_when` / `not_applicable_when` 字段，使适用场景明确区分

---

### Docker 部署时「选择目录注册」选不到宿主机目录

Docker 容器内的文件系统与宿主机隔离，浏览器选出的目录路径是容器内路径，宿主机文件无法直接访问。

**解决方案：** 使用「📤 上传文件初始化」按钮，通过浏览器文件上传接口将宿主机文件传入容器，系统自动转换并写入后端目录或 Vault。

若需挂载宿主机 Obsidian Vault，在 `docker-compose.yml` 中添加：
```yaml
volumes:
  - /path/to/your/obsidian/vault:/vault:rw
```
然后在「Obsidian Vault」弹窗中填入容器内路径 `/vault`。

---

### 上传时提示「conversion failed」

markitdown 转换失败常见原因：
- `.pdf` 需要 `pdfminer.six` 依赖（Docker 镜像中已包含，本机安装需手动安装）
- `.docx` 加密保护的文件无法读取
- 图片扫描件（无文字层）的 PDF 需要 OCR（markitdown 不含 OCR）

处理方式：先手动将文件另存为 `.txt` 或导出为 `.md`，再上传纯文本版本。

---

### 修改分块策略后分析结果没变

修改分块策略后，必须在**设置 → 文档**中点击「重新索引」按钮，否则分析仍基于旧的分块结果。保存策略变更后界面会出现提示，请务必按提示重新索引。

---

### 策略库 Bootstrap 需要运行几次

只需运行一次，接口是幂等的：

```bash
curl -X POST http://localhost:8000/api/v1/extraction/strategies/bootstrap
```

已存在的种子策略不会重复插入。若策略库被意外清空，重新调用即可。

---

### 同名项目注册了两次

系统按 `root_path` 判断项目是否存在，而非项目名称。同一名称、不同路径会创建两个独立项目。若要合并，可手动删除其中一个（确保无审查记录），再重新注册。
