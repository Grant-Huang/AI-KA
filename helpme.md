# AI-KA 使用帮助（Help Me）

## 这个项目是做什么的

AI-KA ProjectLens 用于对实施项目文档做**业务关联性审查**与**结构化分析**。  
你可以把它理解成一个“面向交付与实施场景的文档审查助手”：

- 自动把项目目录中的文档转换为可检索的 Markdown
- 结合你在 `rules.md` 中定义的“关注点（focus）”（名称 + Prompt）对文档做审查
- 对文档进行流式分析，并输出可阅读的结构化结果
- 支持导出分析报告（docx）

典型用途：

- 蓝图评审前：检查需求完整性、接口依赖、主数据一致性
- 上线前：识别风险、进度阻塞与验收缺口
- 复盘阶段：沉淀可复用经验和问题模式

---

## 操作步骤（推荐流程）

1. 点击 **选择项目**，选中项目目录（系统会自动加载）
2. 在首页选择一个或多个**分析关注点**
3. 点击 **开始分析**
4. 在输出区查看：
   - 过程流式输出（大模型按所选关注点审查文档）
   - 后台日志（docs2md、索引、异常信息）
5. 分析完成后可点击 **导出 docx**

---

## 关注点格式说明（rules.md）

系统从 `rules.md` 读取关注点，要求每个关注点是一个块，格式如下：

```md
### focus:req | 需求完整性
这里写这个关注点的 prompt 内容，可多行。
```

说明：

- `focus:req` 是关注点 ID（建议稳定）
- `需求完整性` 是展示名称
- 下方正文是该关注点对应的 Prompt

你也可以在 `rules.md` 增加“组合使用建议”表格，首页会自动显示 Tips：

```md
## 组合使用建议

| 评审节点 | 推荐组合的关注点 |
|---|---|
| 蓝图评审 | `focus:req` + `focus:integration` |
```

---

## Model 配置说明

设置页已分为两个子区域，分别配置：

### 文本大模型

- Provider（通常 `openai_compatible`）
- Text Base URL
- 文本模型（下拉可选，也可直接输入新模型）
- 文本 Key

用于：

- 文档审查与结构化分析主流程（按关注点 Prompt）

### VL 大模型

- VL 模型（下拉可选，也可直接输入新模型）
- VL Base URL（可选，某些服务需要）
- VL Key

用于：

- 图片相关解析（docs2md 的 VL 能力）

---

## 常见问题

### 1) 报错 `http error 404: 404 page not found`

通常是 Base URL 配置不匹配。  
如果你填的是带 `/v1` 的地址，系统现已兼容带/不带 `/v1` 的情况；仍报错时请检查：

- 模型名是否正确
- 网关是否支持 OpenAI-compatible chat 接口
- Key 是否有效

### 2) 为什么后台日志看不到错误

最新版本会把关键异常写入“后台日志”与“过程流式输出”。  
如果仍为空，请先确认流程是否真的启动（是否点击开始分析、是否已选择项目与关注点）。

### 3) 没有配置 VL Key 会怎样

系统会继续执行文本流程，但会在日志中提示“跳过图片解析环节”。

---

## 最佳实践建议

- 先维护好 `rules.md` 中的关注点和 prompt，再做批量分析
- 每次分析前确认模型与 Key 对应关系（文本/VL分开）
- 优先从“组合建议 Tips”里选关注点组合，再按项目特点微调
- 如果输出偏离预期，先检查关注点 prompt 是否过泛或冲突

