# AI-KA 使用帮助

## 功能简介

AI-KA ProjectLens 是一套**通用业务文档审查 + 知识沉淀**平台，由两个主要模块构成。

### 项目审查模块（Review）

- 将项目目录文档（Word、PDF、Markdown 等）转换为可检索的 Markdown 语料
- 结合**审查技能包**中定义的关注点（Focus Points）与 Prompt 对文档做结构化分析
- 流式输出分析里程碑与结论，支持多轮追问
- 自动注入项目记忆（术语、偏好、历史信息），在「思考分析」段显示命中片段
- 审查结束后 LLM 自动提取可泛化的行业/方法论知识到 **Review Queue**

### 知识提取模块（Extraction）

- **Review Queue**：暂存来自项目审查的知识线索，按重复次数排序
- **专家提取对话**：LLM 主动提问（四种策略），满意度驱动多轮澄清
- **规则文档上传**：上传已有规则文档，LLM 做隐性知识澄清
- **待批准规则**：人工审批后写入活动技能包，形成可复用规则
- **内联知识条目审核**：提取对话过程中实时审核 LLM 生成的知识条目（确认 / 修改 / 不采纳）
- **场边教练（Coach Agent）**：每 5 轮对话自动触发分析，向操作员提供提问策略建议
- **自学习飞轮**：策略库 + 元反思机制，会话质量数据驱动 LLM 提问策略持续优化

### 典型用途

| 场景 | 模块 |
| --- | --- |
| 蓝图/需求/上线前快速文档审查 | 项目审查 |
| 复盘阶段提炼可复用经验 | 知识提取（审查后提取） |
| 积累行业最佳实践 | 知识提取（主动提取） |
| 定期更新规则库 | 知识提取（gap_based 策略） |

更多设计细节见仓库内 `docs/design-knowledge-extraction.md`、`docs/design-knowledge-extraction-v2.md` 与 `docs/ProjectLens_需求与设计文档_V1.0.md`。

## 项目审查

### 操作流程

1. 点击左侧 **「项目初始化」**，选择目录（或手动输入路径）注册项目。
2. 点击 **「初始化（转换 + 索引）」** 完成语料准备（docs2md 转换 + 分块索引）。
3. 切换到 **「分析」**，选择已初始化的项目，在输入框描述审查目标（可指定关注点），点击 **「开始」**。
4. 系统自动编排本轮执行：**问题分析 → 评审策略 → 解析文档 → 思考分析 → 呈现结果**。
5. 分析完成后可继续多轮追问；若检测到语料缺失或过期，会提示回到「项目初始化」重建索引。

### 流程区里程碑说明

| 里程碑 | 说明 |
| --- | --- |
| 问题分析 | 理解用户输入，判断是否需要澄清，识别文档类型 |
| 评审策略 | 从活动技能包自动路由适合本次任务的关注点组合 |
| 解析文档 | 召回相关文档分块，准备上下文 |
| 思考分析 | LLM 推理阶段，末尾显示「【加载的记忆】」命中列表 |
| 呈现结果 | 最终结构化结论与导出入口 |

### 记忆（Memory）

- **存放位置**：`.aika/memory/` 下的 `*.md` 文件，按子目录分类（`user/`、`feedback/`、`project/<id>/`、`reference/<id>/`）。
- **怎么注入**：发起分析时后端扫描候选文件，按关键词匹配打分，选出相关片段写入系统提示中的「【注入记忆片段】」段。
- **怎么确认**：「思考分析」折叠段末尾追加「【加载的记忆】」列表，显示本次命中的相对路径与标题。
- **分类说明**：完整分类规范见 `docs/aika_spec/MEMORY_TAXONOMY.md`。

### 审查技能包（Skills）

- **是什么**：可切换的审查配置包，内含 `manifest.json` 与关注点文件（`focus-points/focus-*.md`）及 `review_domain.md`。
- **在哪**：默认在 `review_skill_packages/<包id>/`；可用环境变量 `AIKA_REVIEW_SKILL_PACKAGES_ROOT` 指向其它根目录。
- **切换**：在 **设置 → 审查域** 中切换当前活动包；当前选中的包 id 保存在 `app_settings.md` 的 `active_skill_package_id`。
- **怎么进到模型**：后端将关注点 Prompt、元数据写入系统提示，与文档分块一起发给大模型。

### 预设与会话绑定

- 每次对话在创建时绑定当前的关注点预设组合。
- 打开历史会话时，界面会还原当时绑定的预设，保证回溯一致性。

### 使用提示

- 先维护好活动技能包内的关注点与「组合使用建议」，再跑全流程，结果更稳定。
- 修改分块策略或索引模式后，务必重新执行索引。
- 若结果偏离预期，优先检查：关注点 Prompt 是否过泛、预设组合是否与文档类型匹配。

## 知识提取

### 模块入口

- 左侧导航栏点击**灯泡图标**（🔆）切换到「知识提取」模式，页面以全屏覆盖层展示。
- 顶部有四个标签页：**审查队列 / 主动提取 / 文档上传 / 待批准规则**。
- 完整提取功能（主动提取、文档上传、待批准规则审批）需要 `AIKA_USER_ROLE=senior_expert`。
- `consultant` 角色可查看 Review Queue，但不能发起主动提取或审批规则。

### 专家背景设置（Expert Profile）

1. 首次进入知识提取页时，系统提示填写专家背景。
2. **领域**：多选框，选择擅长的业务/技术方向（如「项目管理」「产品设计」「系统集成」）。
3. **背景描述**：自由文本，描述工作经验与专长，供 LLM 优化提问策略。
4. 填写后点击「保存」，下次进入自动加载（存储于 `~/.aika/memory/user/expert_profile.md`）。

### Review Queue（知识审查队列）

Review Queue 是项目审查与知识提取模块之间的缓冲层，暂存来自项目审查的泛化知识线索。

**线索来源**
- 项目审查结束后，LLM 自动识别结论中可泛化的知识点并写入队列。
- 每条线索包含：关联关注点 ID、建议内容摘要、来源（项目 id / 会话 id）、重复次数。

**查看与使用**
- 在知识提取页左侧列表中查看全部队列项，按重复出现次数降序排列（同一建议多次出现时自动合并计数）。
- LLM 在主动提取对话中会参考队列内容设计问题；直接发起「规则差距」策略时优先覆盖队列里的高优先级项。
- 批准知识条目或明确拒绝后，对应队列项状态更新为 `approved` / `rejected`。

**手动操作**
- 可直接在列表中更改状态（`pending_review` / `in_review` / `approved` / `rejected` / `archived`）。
- 支持删除单条队列项。

### 主动提取对话（四种策略）

在「开始提取」面板选择策略，LLM 将进入多轮对话模式；满意度评分达到 **0.85**（或最多 **5 轮**）后自动生成知识条目。

| 策略 | 适用场景 | LLM 行为 |
| --- | --- | --- |
| **模糊信号（fuzzy_signal）** | 你有一个模糊感觉但说不清楚 | LLM 追问细节，帮你把印象转化为清晰规则 |
| **规则差距（gap_based）** | 想系统梳理现有规则空白 | LLM 对比 Review Queue 与当前规则库，识别未覆盖的模式 |
| **关键事件（critical_incident）** | 有具体案例或事故想提炼规律 | LLM 基于事件细节追问，提炼可复用的经验规则 |
| **反向验证（reverse_validation）** | 想确认/修正一条已有规则 | LLM 提出质疑场景，专家确认或反驳，迭代规则边界 |

**操作步骤**
1. 选择策略，点击「开始」。
2. 在对话框中回答 LLM 提问；若某轮问题不相关，可直接跳过。
3. 对话达到满意度阈值后，LLM 生成一条或多条知识条目并以**内联卡片**形式显示在对话区。
4. 对每张知识卡片执行审核（见下文「内联知识条目审核」）。

### 内联知识条目审核（Sprint 5 新功能）

LLM 每次生成知识条目后，条目以卡片形式直接嵌入对话流中展示，无需跳转页面即可完成审核。

**卡片操作**

| 操作 | 说明 |
| --- | --- |
| **确认** | 直接采纳该条目，标记为 `approved`，后续统一提交到「待批准规则」列表 |
| **修改** | 弹出内联编辑区，可修改标题与内容后确认，状态更为 `edited` |
| **不采纳** | 拒绝该条目，标记为 `rejected`，不进入规则库 |

- 全部卡片处理完毕后，点击「提交审核结果」一次性将 `approved` / `edited` 的条目写入「待批准规则」列表。
- 「重置」按钮清空当前轮次的所有卡片与教练提示，准备下一轮提取。

### 场边教练（Coach Agent，Sprint 6 新功能）

Coach Agent 是一个独立的 LLM 调用，每隔 **5 轮对话**自动触发，分析对话历史，向**操作员**（即你）提供提问策略调整建议。

**教练提示内容**

| 字段 | 说明 |
| --- | --- |
| `whisper` | 核心建议，1–2 句，直接可用的下一轮问题方向 |
| `expert_type_signal` | 当前专家类型推断（如「偏执行派，不善理论化」） |
| `coverage_gaps` | 识别到的知识覆盖空白列表 |
| `current_momentum` | 对当前对话势头的评估（如「对话节奏良好，专家开始主动举例」） |
| `flag` | 风险标记（如「警惕：专家开始给教科书式答案，需要追问具体案例」），可为空 |

**如何使用教练提示**

- 每当教练触发，对话区底部出现**绿色提示条**，显示 `whisper` 建议。
- 点击 **「采纳建议」**：将建议文字预填到输入框，可直接发送或继续修改。
- 点击 **「忽略」**：关闭提示条，继续按自己判断提问。
- 教练提示**仅对操作员可见**，不注入 AI 上下文，不影响 LLM 的对话走向。

> **设计原则**：教练只给你看，你决定用不用。避免 AI 向自己注入偏见。

### 规则文档上传

1. 在知识提取页点击「上传文档」，支持 `.md` / `.txt` / `.docx` / `.pdf`。
2. 上传后 LLM 读取文档内容，基于**现有规则库做知识澄清**：识别文档中与已有关注点重叠或补充的内容。
3. 进入与「主动提取对话」相同的多轮对话流程，满意度驱动终止，同样支持内联知识卡片审核与教练提示。
4. 生成的知识条目进入「待批准规则」列表。

### 审查后提取（Post-Review Extraction）

1. 在「项目审查」模式下完成一轮分析后，分析结果页出现「提取知识」按钮。
2. 点击后 LLM 解析本轮审查结论，自动识别可泛化的行业/方法论知识点。
3. 提取的知识条目写入「待批准规则」列表（同时也可能更新 Review Queue 统计）。

### 待批准规则（Pending Rules）

所有由 LLM 提取、等待人工确认的知识条目集中在「待批准规则」页面。

**列表字段说明**

| 字段 | 说明 |
| --- | --- |
| 关联关注点 | 该知识条目对应的 Focus Point ID |
| 标题 / 内容 | LLM 生成的规则摘要与正文 |
| 置信度 | `high` / `medium` / `low`（LLM 自评） |
| 来源 | 提取策略与原始项目/会话 |
| 冲突 | 与现有规则的冲突检测结果（相似度 > 0.55 或逻辑矛盾） |

**操作**

- **批准（Approve）**：将规则写入活动技能包。若对应关注点已存在，执行 `evolve_focus_point`（版本号 +1，旧版归档到 `.aika/focus-history/`）；若不存在，新建 `focus-points/focus-<id>.md`。
- **拒绝（Reject）**：填写拒绝原因后标记为 `rejected`，不写入规则库。
- **查看冲突**：点击「冲突」标签可展开与哪条现有规则存在冲突，辅助判断是否需要修改规则范围。

### 自学习飞轮（Sprint 6 新功能）

自学习飞轮通过三个时间尺度持续优化 LLM 的提问策略。

#### Scale-1：会话内专家建模（Expert Modeling Protocol）

LLM 在每次对话时，基于专家的回答风格（详细度、举例频率、理论化倾向）动态调整提问策略。专家画像越详细，Scale-1 效果越好。

#### Scale-2A：场边教练（每 5 轮）

见上文「场边教练」章节。

#### Scale-2B：会话后元反思（Meta-Reflection）

会话结束后，系统可对本次提取会话做完整复盘：
- 识别哪些问题产生了高价值知识条目
- 识别哪些提问模式效果差（专家给出泛化答案、对话陷入循环）
- 生成改进建议，更新策略库的有效性评分（EMA 算法：`新分 = 旧分 × 0.85 + 本轮产出率 × 0.15`）

元反思报告保存于后端，可通过 `GET /api/v1/extraction/meta-reflect` 查看历史报告列表。

#### 策略库（Strategy Library）

策略库存储 LLM 提问策略的有效性数据，供 Scale-1 在构建系统提示时注入参考。

- 系统内置 **12 条 Bootstrap 种子策略**，覆盖 4 种策略类型（gap_based / fuzzy_signal / critical_incident / reverse_validation）。
- 首次部署时调用 `POST /api/v1/extraction/strategies/bootstrap` 初始化（幂等操作）。
- 策略有效性分数随元反思更新，高分策略优先注入系统提示。

## 审查域说明

系统从**当前活动审查技能包**读取配置；包根目录默认为 `$AIKA_REPO_ROOT/review_skill_packages`，可用环境变量 `AIKA_REVIEW_SKILL_PACKAGES_ROOT` 覆盖。当前选中的包 id 保存在 `app_settings.md` 的 `active_skill_package_id`（默认 `package-general`）；在 **设置 → 审查域** 可切换（保存后生效）。包目录与 `manifest.json` 约定见 `docs/aika_spec/PACKAGE_LAYOUT.md`。

### 关注点（Focus Point）文件格式

从 v2 起，每个关注点独立存储为技能包下的 `focus-points/focus-<id>.md`，格式如下：

```md
---
schema: aika-focus/1
id: req
name: 需求完整性
version: 2
created_at: 2024-01-01T00:00:00
updated_at: 2024-06-01T00:00:00
applicable_when:
  doc_types:
    - blueprint
    - requirement
not_applicable_when:
  doc_types:
    - report
scope_note: "仅适用于项目前期蓝图与需求类文档"
history:
  - version: 1
    updated_at: 2024-01-01T00:00:00
    note: initial
---
（关注点 Prompt 正文，多行）
```

**字段说明**

| 字段 | 说明 |
| --- | --- |
| `id` | 关注点唯一标识，建议全小写字母+连字符，保持稳定 |
| `name` | 展示名称 |
| `version` | 整数版本号，每次 evolve 自动递增 |
| `applicable_when` | 适用条件（YAML 映射），如文档类型、项目阶段 |
| `not_applicable_when` | 明确排除的条件 |
| `scope_note` | 适用范围的文字说明，注入系统提示供 LLM 参考 |
| `history` | 版本历史列表，每次 evolve 自动追加旧版本记录 |

### 关注点编辑

在 **设置 → 审查域** 中可直接编辑关注点内容；保存后写入当前活动包的 `focus-points/focus-<id>.md`（或 `review_domain.md` 兼容格式）。知识提取审批通过后也会自动更新关注点文件。

### 组合预设（「组合使用建议」）

在 `review_domain.md` 中增加 **二级标题**（标题中须同时包含「组合」以及「建议」「预设」「搭配」「使用」等字样之一），并紧跟 **Markdown 五列表格**：

```md
## 组合使用建议

| 评审节点 | 推荐组合的关注点 | 审查角色 | 审查目标与原则 | 输出要求 |
| --- | --- | --- | --- | --- |
| 蓝图评审 | `focus:req` + `focus:integration` | | | |
| 上线评审 | `focus:risk` + `focus:gap` | | | |
```

- 推荐列使用 `` `focus:<id>` `` 形式，ID 须与 `focus-points/focus-<id>.md` 中的 `id` 字段完全一致。
- 单元格内换行写 HTML `<br>`，单元格内竖线写 `&#124;`。
- 导入或保存后，可在 **设置 → 审查域** 中检查关注点与预设是否与预期一致。

## 设置指南

入口：左侧边栏**齿轮**图标。设置分为三个页签：

| 页签 | 内容 |
| --- | --- |
| **文档** | **分块**：chunk 上限、分块模式（空行分块 / 标题与结构感知）。**图片解析**：是否在 docs2md 转换时解析文档内图片。**文件更新（索引 md_out）**：增量索引或强制全量重建。修改分块或全量策略后，请重新执行索引。 |
| **审查域** | 选择当前**审查技能包**，编辑**关注点**、**组合预设**；支持**导入审查域**从本机选择 Markdown 写入当前活动包。 |
| **模型** | **文本大模型**与 **VL 大模型**：Provider、Base URL、模型名、API Key（文本与 VL 可分别配置）。 |

关注点与部分选项写入当前包的文件；其它应用项落在 `app_settings.md`。**请勿将含密钥的文件提交到公开仓库。**

### 环境变量

| 变量 | 默认值 | 说明 |
| --- | --- | --- |
| `AIKA_USER_ROLE` | `consultant` | 用户角色。`senior_expert` 可访问完整知识提取功能（主动提取、规则审批）；`consultant` 只能使用项目审查。 |
| `AIKA_REPO_ROOT` | 当前工作目录 | 仓库根目录，存放 `.aika/`、`review_skill_packages/` 等。 |
| `AIKA_REVIEW_SKILL_PACKAGES_ROOT` | `$AIKA_REPO_ROOT/review_skill_packages` | 覆盖审查技能包根目录位置。 |

**设置 `AIKA_USER_ROLE` 的方法（示例）**

```bash
# 在启动服务前设置（Linux/macOS）
export AIKA_USER_ROLE=senior_expert

# 或在 .env 文件中添加
AIKA_USER_ROLE=senior_expert
```

### 专家背景（知识提取）

在「知识提取」模式下，通过**专家背景设置**弹窗配置领域与背景描述，存储于 `~/.aika/memory/user/expert_profile.md`，与模型设置独立，重启后自动加载。

## 知识提取 API 参考

以下是后端 REST API 端点（供集成方或高级用户参考）：

| 端点 | 方法 | 说明 |
| --- | --- | --- |
| `/api/v1/extraction/active` | `POST` (SSE) | 主动提取对话流，返回 `status / text / ki / coach_hint / clarify / final` 事件 |
| `/api/v1/extraction/ki/{kid}` | `PATCH` | 更新知识条目（标题、内容、状态） |
| `/api/v1/extraction/ki/{kid}/submit` | `POST` | 提交知识条目到「待批准规则」 |
| `/api/v1/extraction/meta-reflect` | `POST` | 发起会话后元反思分析，保存报告并更新策略有效性分数 |
| `/api/v1/extraction/meta-reflect` | `GET` | 查看元反思历史报告列表 |
| `/api/v1/extraction/strategies` | `GET` | 获取策略库列表（按有效性分数降序） |
| `/api/v1/extraction/strategies` | `POST` | 新建策略条目（管理员用） |
| `/api/v1/extraction/strategies/bootstrap` | `POST` | 初始化 12 条 Bootstrap 种子策略（幂等） |
| `/api/v1/extraction/focus-items` | `GET` | 获取 Review Queue 列表 |
| `/api/v1/extraction/focus-items/{id}` | `PATCH` | 更新 Review Queue 条目状态 |
| `/api/v1/extraction/focus-items/{id}` | `DELETE` | 删除 Review Queue 条目 |

**SSE 事件类型说明**

| 事件类型 | 说明 |
| --- | --- |
| `status` | 阶段进度（如「正在思考…」） |
| `text` | LLM 流式输出文本片段 |
| `ki` | 生成了新知识条目，含 `kid`（条目 ID）与完整 KI 数据 |
| `coach_hint` | 场边教练建议（每 5 轮触发一次），含 `whisper` 等字段 |
| `clarify` | LLM 要求澄清（满意度不足，需继续对话） |
| `final` | 对话轮次结束 |

## 常见问题

### 1) 报错 `http error 404: 404 page not found`

多为 Base URL 与网关路径不一致。若填写带 `/v1` 的地址，请与服务商文档核对；同时检查模型名、兼容接口与 Key。

### 2) 导入审查域后没有「组合预设」

请确认文件中存在**「组合使用建议」**小节及表格，且推荐列使用 `` `focus:关注点id` `` 形式，且与 `focus-points/focus-<id>.md` 的 `id` 字段**完全一致**（含全角符号等细节）。导入成功后可在 **设置 → 审查域** 查看；当前版本分析页系统会自动路由关注点组合。

### 3) 为什么后台日志或流式输出为空

请确认已选择项目与预设，并已启动分析；若模型或网络异常，里程碑或日志中会有提示。

### 4) 没有配置 VL Key 会怎样

文本审查仍可进行；涉及图片解析时可能在日志中提示跳过或降级。

### 5) 打开页面后没有项目、没有选中预设

为默认行为：需自行通过「+」加载目录并选择预设后再开始分析；审查域内容由后端从当前活动包加载（首次初始化默认包时，若尚无 `review_domain.md`，会从内置占位生成）。

### 6) 知识提取页面显示「权限不足」

需将 `AIKA_USER_ROLE` 环境变量设为 `senior_expert` 后重启后端服务。`consultant` 角色只能使用项目审查功能。

### 7) Review Queue 没有内容

Review Queue 的内容来自项目审查后的自动提取。请先完成至少一次项目审查，审查结束后 LLM 会自动将泛化知识线索写入队列。若队列仍为空，请检查后端日志中是否有提取错误。

### 8) 批准规则后在审查域中找不到

批准后规则写入当前活动技能包的 `focus-points/` 目录（新关注点）或对现有关注点执行版本升级。请在 **设置 → 审查域** 刷新页面，或检查 `review_skill_packages/<包id>/focus-points/` 目录。

### 9) 冲突检测说与某条规则冲突，但我认为不冲突

冲突检测基于文本相似度启发式规则，存在误报。可直接点击「批准」忽略冲突提示；或在批准前先在审查域中调整两条规则的 `applicable_when` / `not_applicable_when` 字段，使它们的适用范围明确区分，冲突检测下次即可正确区分。

### 10) 如何查看关注点的历史版本

关注点每次升级后旧版本归档到 `.aika/focus-history/focus-<id>-v<n>.md`；可直接读取该文件查看历史内容。当前界面不提供历史版本对比 UI，可用文本工具（如 `diff`）手动比对。

### 11) 场边教练不出现

教练每 **5 轮**触发一次（计数从第 1 轮开始）。若 5 轮后未出现教练提示，请检查后端日志中是否有 `coach` 相关错误；教练分析失败会静默跳过，不影响主流程。

### 12) 内联知识卡片如何批量处理

目前暂不支持批量一键确认；需对每张卡片分别操作（确认 / 修改 / 不采纳）。全部处理完毕后点击「提交审核结果」一次性写入。若要快速跳过某轮全部卡片，可点击每张卡片上的「不采纳」后统一提交。

### 13) 元反思（Meta-Reflection）需要手动触发吗

是的，当前版本需要手动调用 `POST /api/v1/extraction/meta-reflect`，传入会话 ID 与 KI 列表。界面端尚未集成触发按钮（计划在后续 Sprint 中加入）。

### 14) 策略库 Bootstrap 需要运行几次

只需运行一次（幂等）：`POST /api/v1/extraction/strategies/bootstrap`。已存在的种子策略不会被重复插入。建议在首次部署后立即执行一次。
