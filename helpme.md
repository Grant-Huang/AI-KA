# AI-KA 使用帮助

## 功能简介

AI-KA ProjectLens 是一个**通用的业务关联性文档审查**与**结构化分析**工具。你可以把它理解成「可配置的文档审查助手」：系统依据 `rules.md` 中定义的关注点与 Prompt 进行审查，因此不仅适用于项目交付实施，也可用于流程制度、产品需求、合规材料、运营规则、接口契约等业务文档。

**能力概览**

- 将项目目录中的文档转换为可检索的 Markdown（docs2md 等管线）
- 结合 `rules.md` 中的「关注点（focus）」（名称 + Prompt）对文档做审查
- 流式输出分析过程与结构化结论，并支持导出（如 Markdown）

**典型用途（以项目交付类场景为例）**

- 蓝图评审前：检查需求完整性、接口依赖、主数据一致性  
- 上线前：识别风险、进度阻塞与验收缺口  
- 复盘阶段：沉淀可复用经验与问题模式  

更多设计与扩展思路见仓库内 `README.md` 与 `docs/ProjectLens_需求与设计文档_V1.0.md`。

## 操作流程

1. 点击输入栏左侧 **「+」**（提示为「加载文件或目录」），选择要审查的目录或文件；系统会注册/加载对应项目。  
2. 在 **「选择预设」** 中任选一组关注点组合（必选）。预设可来自 `rules.md` 中 **「组合使用建议」** 表格的自动推导，也可在 **设置 → 规则** 中手动维护。  
3. 可选：在输入框中填写本次任务说明。  
4. 点击 **「开始」**（右侧运行图标；运行中同一位置为「终止」）。  
5. 在输出区查看：过程流式输出、里程碑（文档转换、索引与分块、模型分析等，可展开）、以及后台日志与异常提示。  
6. 分析完成后可 **复制 / 导出 Markdown**；若已创建对话，可在结果区 **追问** 以继续补充分析。

**说明**：需先通过左侧 **「+」**（新对话）创建会话后，结果区的追问输入框才会启用。

**使用提示（精简）**

- 先维护好 `rules.md` 中的关注点与「组合使用建议」，再跑全流程，结果更稳定。  
- 修改 **分块策略或索引模式** 后，务必重新执行 **索引**（及后续分析），否则结论仍可能基于旧分块。  
- 在 **设置 → 模型** 中核对文本 / VL 的 Base URL、模型名与 Key 是否与环境一致。  
- 若审查结果偏离预期，优先检查：关注点 Prompt 是否过泛、预设组合是否与当前文档类型匹配。

## 规则说明

系统从**当前启用的规则文件**读取配置（环境变量 `AIKA_RULES_FILENAME`，默认仓库根目录下 `rules.md`）。多个规则文件彼此独立，**同时只启用其一**；切换文件名需改环境变量并**重启后端**。

**关注点（focus）**

每个关注点一个块，格式示例：

```md
### focus:req | 需求完整性
这里写该关注点对应的 Prompt，可多行。
```

- `focus:req`：关注点 ID（建议保持稳定，并与下方表格中 `` `focus:...` `` 引用一致）  
- `需求完整性`：展示名称  
- 正文：该关注点对应的 Prompt  

**组合预设（「组合使用建议」）**

在规则文件中增加 **二级标题**（标题中须同时包含「组合」以及「建议」「预设」「搭配」「使用」等字样之一），并紧跟 **Markdown 表格**。系统仅解析 **固定五列表**（列序如下），并将表格行映射为首页与设置中的 **组合预设**（从 `` `focus:id` `` 解析关注点）。后三列用于 **审查角色**、**审查目标与原则**、**输出要求**，可留空；单元格内换行请写为 HTML `<br>`，单元格内需出现竖线 `|` 时请写为 `&#124;`。

示例：

```md
## 组合使用建议

| 评审节点 | 推荐组合的关注点 | 审查角色 | 审查目标与原则 | 输出要求 |
| --- | --- | --- | --- | --- |
| 蓝图评审 | `focus:req` + `focus:integration` | | | |
```

导入或保存规则后，可在 **设置 → 规则** 中检查关注点与预设是否与预期一致。

## 设置指南

入口：左侧边栏 **齿轮** 图标。设置分为三个页签：

| 页签 | 内容 |
|------|------|
| **文档** | **分块**：chunk 上限、分块模式（空行分块 / 标题与结构感知）。**图片解析**：是否在 docs2md 转换时解析文档内图片。**文件更新（索引 md_out）**：增量索引或强制全量重建。修改分块或全量策略后，请按界面提示重新执行索引后再解读分析结果。 |
| **规则** | 从当前规则文件加载并编辑 **关注点**、**组合预设**；支持 **加载 rules** 从本机选择文件导入。 |
| **模型** | **文本大模型** 与 **VL 大模型**：Provider、Base URL、模型名、API Key（文本与 VL 可分别配置）。 |

关注点与部分选项会写入规则侧配置；其它应用项可能落在 `app_settings.md`（以当前版本行为为准）。**请勿将含密钥的文件提交到公开仓库。**

## 常见问题

### 1) 报错 `http error 404: 404 page not found`

多为 Base URL 与网关路径不一致。若填写带 `/v1` 的地址，请与服务商文档核对；同时检查模型名、兼容接口与 Key。

### 2) 导入 rules 后没有「组合预设」

请确认文件中存在 **「组合使用建议」** 小节及表格，且推荐列使用 `` `focus:关注点id` `` 形式，且与上文 `### focus:...` 的 ID **完全一致**（含全角符号等细节）。导入成功后可在 **设置 → 规则** 查看；首页 **选择预设** 需**手动**在下拉框中选取，系统不会默认选中任一项。

### 3) 为什么后台日志或流式输出为空

请确认已选择项目、预设，并已启动分析；若模型或网络异常，里程碑或日志中会有提示。

### 4) 没有配置 VL Key 会怎样

文本审查仍可进行；涉及图片解析时可能在日志中提示跳过或降级。

### 5) 打开页面后没有项目、没有选中预设

为默认行为：需自行通过 **「+」** 加载目录并 **选择预设** 后再开始分析；规则内容仍由后端按 `rules.md`（或 `AIKA_RULES_FILENAME` 指定文件）加载。
