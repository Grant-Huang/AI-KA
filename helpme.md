# AI-KA 使用帮助

## 功能简介

AI-KA ProjectLens 是一套**通用业务文档审查 + 知识沉淀**平台，由两个主要模块构成。

### 项目审查模块（Review）

- 将项目目录文档（Word、PDF、Markdown 等）转换为可检索的 Markdown 语料
- 结合**审查技能包**中定义的关注点（Focus Points）与 Prompt 对文档做结构化分析
- 流式输出分析里程碑与结论，支持多轮追问
- 自动注入项目记忆（术语、偏好、历史信息），在「思考分析」段显示命中片段
- 审查结束后 LLM 自动提取可泛化的行业/方法论知识到 **审查队列**

### 知识提取模块（Extraction）

- **提取工作台**：三种开始方式（审查队列条目 / 自由提取 / 文档澄清），AI 主动提问
- **待批准规则**：人工审批后写入活动技能包，形成可复用规则
- **内联知识条目审核**：提取对话过程中实时审核 LLM 生成的知识条目
- **场边教练（Coach Agent）**：每 5 轮对话触发，向操作员提供提问策略建议
- **自学习飞轮**：策略库 + 元反思机制持续优化 LLM 提问策略

### 典型用途

| 场景 | 模块 |
| --- | --- |
| 蓝图/需求/上线前快速文档审查 | 项目审查 |
| 复盘阶段提炼可复用经验 | 知识提取（审查后提取） |
| 积累行业最佳实践 | 知识提取（自由提取） |
| 定期更新规则库 | 知识提取（规则差距策略） |

更多设计细节见仓库内 `docs/design-knowledge-extraction-v2.md`。

## 项目审查

### 操作流程

1. 点击左侧 **「项目初始化」**，选择目录注册项目并执行初始化。
2. 切换到 **「分析」**，选择项目，描述审查目标，点击「开始」。
3. 系统编排：**问题分析 → 评审策略 → 解析文档 → 思考分析 → 呈现结果**。
4. 分析完成后可继续多轮追问；审查结束后点击「提取知识」进入知识提取模式。

### 审查队列（Review Queue）入口

左侧导航栏点击 **列表图标** 可打开「审查队列」侧边抽屉，查看项目审查自动提取的知识线索：

- **来源**：每次项目审查结束后，LLM 自动识别结论中可泛化的知识点写入队列
- **Trust 标识**：资深顾问审查产生的线索标记为「中信任」，普通顾问为「低信任」
- **操作**：归档（暂时忽略）/ 开始提取（切换到知识提取工作台，自动携带该条目）
- **角标**：左侧导航按钮上显示当前待审查条目数量

### 里程碑说明

| 里程碑 | 说明 |
| --- | --- |
| 问题分析 | 理解用户输入，识别文档类型 |
| 评审策略 | 从活动技能包自动路由适合的关注点组合 |
| 解析文档 | 召回相关文档分块 |
| 思考分析 | LLM 推理，末尾显示「【加载的记忆】」命中列表 |
| 呈现结果 | 最终结论与导出入口 |

### 记忆（Memory）

- **存放位置**：`.aika/memory/` 下的 `*.md` 文件（`user/`、`project/<id>/` 等子目录）
- **怎么注入**：按关键词匹配打分，命中片段写入系统提示
- **怎么确认**：「思考分析」末尾追加「【加载的记忆】」列表

### 审查技能包（Skills）

- 默认路径：`review_skill_packages/<包id>/`
- 切换：**设置 → 审查域** 中选择当前活动包
- 内容：`manifest.json` + 关注点文件（`focus-points/focus-*.md`）+ `review_domain.md`

## 知识提取

### 模块入口

左侧导航栏点击**灯泡图标**切换到「知识提取」模式，页面全屏覆盖展示，顶部两个标签页：

| 标签 | 功能 |
| --- | --- |
| **提取工作台** | 三种开始方式的统一会话界面 |
| **待批准规则** | 审批 LLM 提取的知识条目，写入规则库 |

> 完整提取功能（提取、审批）需要 `AIKA_USER_ROLE=senior_expert`。

### 专家入职访谈（首次进入）

**首次**进入知识提取时，系统显示三步引导访谈（而非直接进入工作台）：

1. **Step 1 — 专业领域**：从领域列表中多选擅长方向。
   - 领域选项根据当前审查队列的关注点分布**动态生成**（与队列内容相关的领域优先显示）
   - 至少选择一个领域才能进入下一步

2. **Step 2 — 工作背景（可选）**：描述工作年限、典型项目类型、常遇挑战等，供 LLM 优化提问角度

3. **Step 3 — 确认**：预览画像摘要，点击「保存并开始提取」完成初始化

画像保存后自动进入工作台；可随时通过右上角「专家画像」按钮修改。

### 提取工作台（三种开始方式）

工作台顶部有一个分段选择器，切换不同的开始方式：

| 方式 | 场景 | 操作 |
| --- | --- | --- |
| **从审查队列** | 针对具体知识线索深入提炼 | 从下拉框选择队列条目 + 选择策略 |
| **自由提取** | 主动提炼某个领域经验 | 直接选择策略 + 描述主题 |
| **文档澄清** | 上传已有规则文档做知识澄清 | 拖拽上传 .md/.txt/.docx/.pdf |

三种方式共用同一个对话界面（ChatArea + 知识条目卡片 + 教练提示）。

#### 四种提取策略

| 策略 | 适用场景 |
| --- | --- |
| **规则差距** | 对比审查队列与现有规则，识别空白 |
| **模糊信号** | 澄清模糊印象，转化为清晰规则 |
| **关键事件** | 从具体案例提炼可复用规律 |
| **反向验证** | 验证或反驳现有规则的适用边界 |

#### 满意度进度

每轮对话结束后，ChatArea 下方显示**满意度进度条**：

- 橙色（< 85%）：LLM 认为知识尚未完整澄清，建议继续追问
- 绿色（≥ 85%）：满意度达标，自动生成知识条目
- 出现「本条知识已完整提炼 ✓」绿色提示时，表示该条目可以提交审批

### 内联知识条目审核

LLM 每次生成知识条目后，以卡片形式直接嵌入对话流中展示：

| 操作 | 说明 |
| --- | --- |
| **确认入库** | 采纳该条目，标记为 `approved`，写入「待批准规则」 |
| **修改** | 弹出编辑弹窗，修改标题/内容后确认，状态为 `edited` |
| **不采纳** | 拒绝该条目，不进入规则库 |

### 场边教练（Coach Agent）

每隔 **5 轮对话**自动触发，对话区底部出现绿色教练提示条：

| 字段 | 说明 |
| --- | --- |
| 核心建议（whisper） | 1–2 句，建议的下一轮提问方向 |
| 专家类型推断 | 如「偏执行派，不善理论化」 |
| 未覆盖领域 | 识别到的知识空白列表 |
| 对话势头 | 「停滞」/「减弱」时显示警告标签 |
| 风险标记（flag） | 如「警惕：专家开始给教科书式答案」 |

- 点击 **「采纳建议」**：whisper 内容预填到输入框，可直接发送或修改
- 点击 **「忽略」**：关闭提示，继续自主提问
- 教练提示**仅对操作员可见**，不注入 AI 上下文

### 审查后提取（Post-Review Extraction）

在审查结果页点击「提取知识」，切换到知识提取模式并自动进入「审查后提取」状态：

- LLM 解析本次审查结论，识别可泛化知识点
- 点击「开始提取」自动发起第一轮
- 支持多轮追问、内联 KI 卡片审核、教练提示

### 待批准规则（Pending Rules）

所有由 LLM 提取、等待人工确认的知识条目集中在「待批准规则」标签页。

**操作**
- **批准**：将规则写入活动技能包（新关注点或现有关注点版本升级）
- **拒绝**：填写拒绝原因后标记 `rejected`，不写入规则库
- **展开行**：查看完整内容与适用范围

### 自学习飞轮（三层机制）

| 层级 | 时间尺度 | 实现 |
| --- | --- | --- |
| Scale-1 | 会话内 | 专家建模协议（Expert Modeling Protocol）注入系统提示 |
| Scale-2A | 每 5 轮 | 场边教练 Agent（Coach）分析对话历史给出建议 |
| Scale-2B | 会话后 | 元反思（Meta-Reflection）复盘，更新策略有效性分数 |

策略库内置 12 条 Bootstrap 种子策略；首次部署调用 `POST /api/v1/extraction/strategies/bootstrap` 初始化。

## 审查域说明

系统从**当前活动审查技能包**读取配置；可用环境变量 `AIKA_REVIEW_SKILL_PACKAGES_ROOT` 覆盖根目录。

### 关注点文件格式

```md
---
schema: aika-focus/1
id: req
name: 需求完整性
version: 2
applicable_when:
  doc_types: [blueprint, requirement]
scope_note: "仅适用于项目前期蓝图与需求类文档"
---
（关注点 Prompt 正文）
```

### 组合预设

在 `review_domain.md` 中加二级标题（含「组合」+「建议/预设」等字样）并紧跟五列表格：

```md
## 组合使用建议

| 评审节点 | 推荐组合的关注点 | 审查角色 | 审查目标与原则 | 输出要求 |
| --- | --- | --- | --- | --- |
| 蓝图评审 | `focus:req` + `focus:integration` | | | |
```

## 设置指南

左侧边栏**齿轮**图标进入设置，分三个页签：

| 页签 | 内容 |
| --- | --- |
| **文档** | 分块策略、图片解析、增量/全量索引 |
| **审查域** | 切换技能包，编辑关注点与组合预设，导入审查域 |
| **模型** | 文本大模型与 VL 大模型配置（Provider、Base URL、API Key） |

### 环境变量

| 变量 | 默认值 | 说明 |
| --- | --- | --- |
| `AIKA_USER_ROLE` | `consultant` | `senior_expert` 可访问完整知识提取功能 |
| `AIKA_REPO_ROOT` | 当前工作目录 | 仓库根目录 |
| `AIKA_REVIEW_SKILL_PACKAGES_ROOT` | `$AIKA_REPO_ROOT/review_skill_packages` | 覆盖技能包根目录 |

## 知识提取 API 参考

| 端点 | 方法 | 说明 |
| --- | --- | --- |
| `/api/v1/extraction/active` | `POST` (SSE) | 主动提取会话流 |
| `/api/v1/extraction/doc` | `POST` (SSE) | 文档澄清会话流 |
| `/api/v1/extraction/review` | `POST` (SSE) | 审查后提取会话流 |
| `/api/v1/extraction/ki/{kid}` | `PATCH` | 更新知识条目（标题、内容、状态） |
| `/api/v1/extraction/ki/{kid}/submit` | `POST` | 提交知识条目到「待批准规则」 |
| `/api/v1/extraction/meta-reflect` | `POST/GET` | 发起/查看会话后元反思 |
| `/api/v1/extraction/strategies` | `GET/POST` | 策略库增删查 |
| `/api/v1/extraction/strategies/bootstrap` | `POST` | 初始化 12 条种子策略（幂等） |
| `/api/v1/review-queue` | `GET` | 获取审查队列列表 |
| `/api/v1/review-queue/{id}` | `PATCH/DELETE` | 更新/删除队列条目 |

**SSE 事件类型**

| 事件 | 说明 |
| --- | --- |
| `status` | 阶段进度文字 |
| `text` | LLM 流式输出片段 |
| `ki` | 新知识条目（含 kid 和完整数据） |
| `coach_hint` | 场边教练建议（每 5 轮触发） |
| `clarify` | LLM 要求澄清，含建议问题列表 |
| `final` | 轮次结束，含满意度分数与 auto_advance 标志 |

## 常见问题

### 1) 报错 `404 page not found`

多为 Base URL 与网关路径不一致。检查 **设置 → 模型** 中的 Base URL 与 API Key 是否正确。

### 2) 导入审查域后没有「组合预设」

确认文件中有「组合使用建议」二级标题及五列表格，推荐列使用 `` `focus:<id>` `` 格式，与 `focus-points/focus-<id>.md` 的 `id` 字段完全一致。

### 3) 知识提取页面显示「权限不足」

设置 `AIKA_USER_ROLE=senior_expert` 后重启后端服务。

### 4) 审查队列没有内容

审查队列来自项目审查后的自动提取。请先完成至少一次项目审查。若队列仍为空，检查后端日志中是否有提取错误。

### 5) 首次进入知识提取没有看到访谈流程

如果专家画像已存在（`~/.aika/memory/user/expert_profile.md` 或后端记录非空），系统会跳过访谈直接进入工作台。清空画像（右上角「专家画像」→ 清空领域并保存）即可重新触发访谈流程。

### 6) 场边教练不出现

教练每 5 轮触发一次。如 5 轮后仍未出现，检查后端日志中的 `coach` 相关错误；教练分析失败会静默跳过，不影响主流程。

### 7) 满意度进度条显示 0% 但已回答多轮

满意度由 LLM 内部评估，取决于：规则表述是否清晰、适用范围是否界定、是否发现反例、置信度是否可确定。尝试更具体地描述场景、举出例外情况，满意度会上升。

### 8) 批准规则后在审查域中找不到

规则写入当前活动技能包的 `focus-points/` 目录。在 **设置 → 审查域** 刷新页面，或检查 `review_skill_packages/<包id>/focus-points/` 目录。

### 9) 冲突检测误报

冲突检测基于文本相似度启发式规则。可直接点击「批准」忽略冲突提示；或先调整两条规则的 `applicable_when` / `not_applicable_when` 字段使适用范围明确区分。

### 10) 策略库 Bootstrap 需要运行几次

只需运行一次（幂等）：`POST /api/v1/extraction/strategies/bootstrap`。已存在的种子策略不会重复插入。
